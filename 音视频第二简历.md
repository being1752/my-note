# 音视频第二简历

------

## 个人技能

### 新特性

[C++11全部新特性](https://cloud.tencent.com/developer/article/1745592)

[C++14全部新特性](https://zhuanlan.zhihu.com/p/165389083)

[C++17全部新特性](https://zhuanlan.zhihu.com/p/586762990)



### 智能指针

智能指针是C++11引入的类模板，用于管理资源，行为类似于指针，但不需要手动申请、释放资源，所以称为智能指针。

智能指针底层使用的引用计数是`atomic`的原子变量，原子变量在自增自减时是线程安全的，这保证了多线程读写智能指针时是安全的。

#### auto_ptr

auto_ptr是C++标准库中`(<utility>`)为了解决资源泄漏的问题提供的一个智能指针类模板（注意：这只是一种简单的智能指针）
auto_ptr的实现原理其实就是RAII（Resource Application Immediately Initialize），在构造的时候获取资源，在析构的时候释放资源，并进行相关指针操作的重载，使用起来就像普通的指针。

为了避免重复释放，在auto_ptr实例之间赋值时，会转移托管指针的控制权，即在赋值完成后，托管指针变为nullptr。这一特性实际用处不大又不符合常规认知，因此在C++11中已经把auto_ptr给废除了

#### unique_ptr

**std::unique_ptr** 对其持有的堆内存具有唯一拥有权，也就是说引用计数永远是 1，**std::unique_ptr** 对象销毁时会释放其持有的堆内存。

**std::unique_ptr** 禁止复制语义，为了达到这个效果，**std::unique_ptr** 类的拷贝构造函数和赋值运算符（operator =）被标记为 **delete。**

尽量使用`std::unique_ptr<int> sp3 = std::make_unique<int>(123);`方式去创建一个 std::unique_ptr。



#### shared_ptr

**std::shared_ptr** 持有的资源可以在多个 **std::shared_ptr** 之间共享，每多一个 **std::shared_ptr** 对资源的引用，资源引用计数将增加 1，每一个指向该资源的 **std::shared_ptr** 对象析构时，资源引用计数减 1，最后一个 **std::shared_ptr** 对象析构时，发现资源计数为 0，将释放其持有的资源。

多个线程之间，递增和减少资源的引用计数是安全的，使用了原子操作，但这不意味着多个线程同时操作 **std::shared_ptr** 引用的对象是安全的。

引用计数在shared_ptr里是通过指针的方式，存储在基类的_Ref_count_base里

[MSVC源码解读](https://kingsamchen.github.io/2018/03/16/demystify-shared-ptr-and-weak-ptr-in-msvc-stl/)



#### weak_ptr

**std::weak_ptr** 是一个不控制资源生命周期的智能指针，是对对象的一种弱引用，只是提供了对其管理的资源的一个访问手段，引入它的目的为协助 **std::shared_ptr** 工作。

**std::weak_ptr** 可以从一个 **std::shared_ptr** 或另一个 **std::weak_ptr** 对象构造，**std::shared_ptr** 可以直接赋值给 **std::weak_ptr** ，也可以通过 **std::weak_ptr** 的 **lock()** 函数来获得 **std::shared_ptr**。它的构造和析构不会引起引用计数的增加或减少。**std::weak_ptr** 可用来解决 **std::shared_ptr** 相互引用时的死锁问题（即两个**std::shared_ptr** 相互引用，那么这两个指针的引用计数永远不可能下降为 0， 资源永远不会释放）。

**std::weak_ptr** 的应用场景，**经典的例子是订阅者模式或者观察者模式中**。这里以订阅者为例来说明，消息发布器只有在某个订阅者存在的情况下才会向其发布消息，而不能管理订阅者的生命周期。



### STL

#### STL的六大组件

1. 容器（Containers）：各种数据结构，如Vector,List,Deque,Set,Map,用来存放数据，STL容器是一种Class Template,就体积而言，这一部分很像冰山载海面的比率。
2. 算法（Algorithms）：各种常用算法如Sort,Search,Copy,Erase,从实现的角度来看，STL算法是一种Function Templates。
3. 迭代器（Iterators）：扮演容器与算法之间的胶合剂，是所谓的“泛型指针”，共有五种类型，以及其它衍生变化，从实现的角度来看，迭代器是一种将：Operators*,Operator->,Operator++,Operator--等相关操作予以重载的Class Template。所有STL容器都附带有自己专属的迭代器——是的，只有容器设计者才知道如何遍历自己的元素，原生指针（Native pointer）也是一种迭代器。
4. 仿函数（Functors）：行为类似函数，可作为算法的某种策略（Policy）,从实现的角度来看，仿函数是一种重载了Operator()的Class 或 Class Template。一般函数指针可视为狭义的仿函数。
5. 适配器（配接器）（Adapters）：一种用来修饰容器（Containers）或仿函数（Functors）或迭代器（Iterators）接口的东西，例如：STL提供的Queue和Stack，虽然看似容器，其实只能算是一种容器配接器，因为 它们的底部完全借助Deque，所有操作有底层的Deque供应。改变Functor接口者，称为Function Adapter;改变Container接口者，称为Container Adapter;改变Iterator接口者，称为Iterator Adapter。配接器的实现技术很难一言蔽之，必须逐一分析。
6. 分配器（Allocators）：负责空间配置与管理，从实现的角度来看，配置器是一个实现了动态空间配置、空间管理、空间释放的Class Template。



#### array

一个固定大小的数组，**在栈上分配**，**元素类型及大小在声明的时候指定**

与vector相比，不能自动扩容，长度在刚刚分配的时候就固定了



#### vector

**动态大小的数组**，同时使用`size`和`capacity`记录当前元素的数量和当前动态数组的容量。如果持续的`push_back(emplace_back)`元素，当`size`大于`capacity`时，需要开辟一块更大的动态数组，并把旧动态数组上的元素搬移到当前动态数组，然后销毁旧的动态数组

新开辟的动态数组的容量：**MSVC 是1.5，而GCC是2**

知道数据的大概量，我们可以使用`reserve`方法直接为`vector`扩容这个量级。这样在后续的数据插入时就不会因为频繁的`capacity`被用尽而导致的多次的数据搬移，从而提升`vector`插入效率

push_back和emplace_back两者都可以在容器尾部插入元素。push_back() 向容器尾部添加元素时，首先会创建这个元素，然后再将这个元素拷贝或者移动到容器中（如果是拷贝的话，事后会自行销毁先前创建的这个元素）；而 emplace_back() 在实现时，则是直接在容器尾部创建这个元素，省去了拷贝或移动元素的过程。当C++版本高于C++11时，`emplace_back`返回当前插入的值的引用，而`push_back`返回`void`。

`erase`属于成员函数，`erase`删除了元素，`remove`属于算法库函数，而`remove`只会把元素移动到尾部。

迭代器失效主要有两种情况引起：1.插入数据。由于插入数据可能导致数据搬移（`size > capacity`）,所以迭代器失效。2.删除数据。当使用`erase`删除数据时，被删除数据后面的数据依次向前移一位。这会导致被删除数据之后的迭代器失效。

快速的清空`vector`容器并释放`vector`容器所占用的内存：

1. 第一种，使用`clear`方法清空所有元素。然后使用`shrink_to_fit`方法把`capacity`和`size（0）`对齐，达到释放内存的作用：
2. 第二种，使用`swap`方法；

`vector<bool>`的实现和其他实现容器的实现不一致。每个元素被当作一个位而不是一个字节存储。这导致我们不能直接访问该元素，也无法对每个元素取地址（`8`个元素可能在同一个字节中存储）。所以不建议使用`vector<bool>`，必要时可以使用`std::bitset`替代。



#### list

C++双向链表，⽀持在任意位置快速插⼊和删除，但不⽀持随机访问。由于它的节点分散在 内存中，扩容不会影响已有元素的地址。

list头部有一个`header node`，从C++11开始，此`node`中包含一个`size_t`类型的成员变量，用来记录`list`的长度，其他`node`并没有记录。所以说从C++11开始，`size()`的时间复杂度是`O(1)`，在此之前是`O(N)`。

添加和删除元素不会导致迭代器失效，因为在任意位置添加和删除元素只需要改变`prev/next`指针指向的对象，而不需要移动元素的位置，所以不会导致迭代器失效。



#### forward_list

顺序容器（可顺序访问，但不支持随机访问），单链表，使用内存分配器动态管理内存 ；

forward_list 相比于 list 的核心区别是它是一个单链表，因此每个元素只会与相邻的下一个元素关联！由于关联信息少了一半，因此 forward_list 占用的内存空间更小，且插入和删除的效率稍稍高于 list。作为代价，forward_list 只能单向遍历。

设计 forward_list 的目的是为了达到不输于任何一个C风格手写链表的极值效率！为此，forward_list 是一个最小链表设计，它甚至没有`size()`接口，因为内部维护一个size变量会降低增删元素的效率。如果想要获取 forward_list 的 size，一个通常的做法是，用 std::distance 计算 begin 到 end 的距离得出 size。一句话总结：list 兼顾了接口丰富性牺牲了效率，而 forward_list 舍弃了不必要的接口只为追求极致效率。

#### deque

deque是一个双向开口的容器，所谓双向开口就是再头尾两端均可以做元素的插入和删除操作。

`deque`是部分连续的。`deque`通常维护了一个`map`（不是`std::map`），`map`的每个元素指向一个固定大小的`chunk`。同时维护了两个指针，指向头`chunk`和尾`chunk`。在`deque`的头部或尾部插入元素时，`deque`会找到头部或尾部的指针，并通过指针找到对应的`chunk`。如果`chunk`中还有未被元素填充的位置，则将元素填充到数组中，如果此指针指向的`chunk`已经被元素填满，则需要重新开辟一块固定大小的`chunk`，并将`chunk`记录在`map`中。

`dqueue`查找的时间复杂度是`O(N)`，插入要分情况，如果是头插和尾插，时间复杂度为`O(1)`，如果是中间插入，则是`O(N)`。删除元素和插入元素的时间复杂度相同。



#### stack

栈，动态管理尺⼨。默认情况下，`stack`使用`deque`作为其底层容器，但也可以使用`vector`或`list`作为底层容器。

`deque`虽然遍历效率不如`vector`、随机插入效率不然`list`，但`stack`和`queue`不需要遍历以及在`stack`中元素增长时，`deque`比`vector`的效率高(扩容时不需要搬移大量数据)，所以`deque`是`stack`底层容器的最佳选择。



#### queue

队列，动态管理尺⼨，先进先出型容器



#### pirority_queue

优先队列（priority_queue）容器与队列一样，只能从队尾插入元素，从队首删除元素。但是它有一个特性，队列中最大的元素总是位于队首。出队时，并非按照先进先出的原则进行，而是将当前队列中最大的元素出队。这点类似于给队列里的元素进行了由大到小的顺序排序。元素的比较规则默认按元素值由大到小排序，可以重载“<”操作符来重新定义比较规则。**在优先队列中，队首元素一定是当前队列中优先级最高的那一个。**



#### set

`std::set`是一个有序的集合，其中的元素是唯一的，即每个元素只能出现一次。一般用于去重和自动排序。

set 是直接保存 value 的，或者说，set 中的 value 就是 key。



#### map

`std::map`是有序组合，它不止有`key`，每个`key`还对用一个`value`。其中`key`是唯一，不可重复，但是`value`却没有限制。`key/value`也被称为键值对。

map 通过底层的**「红黑树」**数据结构来将所有的元素按照 key 的相对大小进行排序，所实现的排序效果也是严格弱序特性（strict weak ordering）

`map`中的`key`是`const`的。强制修改（取地址，`const_cast`转非`const`指针，解引用赋值）会造未知的错误



#### unordered_set

在 unordered_set 中，元素自身同时也作为 key 使用；既然是作为 key 使用，那么元素就不能被更改，也即 unordered_set 中的元素都是 constant 的，但我们可以自由的插入和删除元素，这也是所有`XXX_set`类容器的性质。既然底层结构是哈希表，意味着 unordered_set 中的元素是无序的，不能按照大小排序，这也是所有`unordered_XXX`类容器的性质。



#### unordered_map

关联容器，unordered_map 以**哈希表**（hash table）作为底层数据结构来组织数据，以键值对儿 <key, value> 作为元素进行存储



## 多线程与多进程

### 进程

#### 定义

- 进程是处于执⾏期的程序以及它所管理的资源（如打开的⽂件、挂起的信号、进程状态、地址 空间等等）的总称。每个线程有⼀个独⽴的虚拟地址空间，⽤来容纳进程映像（如与进程关联 的程序与数据），并以进程为单位对各种资源实施保护，如受保护地访问处理器、⽂件、外部 设备及其他进程（进程间通信）
- 每个进程的内存空间都由保存全局变量的“数据区”、向malloc等函数的动态分配提供空间的堆 (Heap)、⼊函数运⾏时使⽤的栈(Stack)构成。每个进程都拥有这种独⽴空间
- 进程从来不执⾏任何东⻄,它只是纯种的容器,若要使进⾏完成某项操作,它必须拥有⼀个在它的 环境中运⾏的纯种,此线程负责执⾏包含在进程的地址空间的中的代码.也就是,真正完成代码执 ⾏的是线程,⽽进程只是纯种的容器,或者说是线程的执⾏环境。
- 和线程不同的是，⼦进程实际上就是另⼀个进程，其是否结束与创建它的⽗进程是否结束⽆关
- CPU的核⼼数与同时可运⾏的进程数相同，若运⾏的进程数超过CPU的核⼼数，则会以分时 使⽤的⽅式调度CPU资源，此时就会涉及到进程的上下⽂切换
- 上下⽂切换(Context Switch)：就是先把前⼀个任务的 CPU 上下⽂（也就是 CPU 寄存器和 程序计数器）保存起来，然后加载新任务的上下⽂到这些寄存器和程序计数器，最后再跳转到 程序计数器所指的新位置，运⾏新任务。⽽这些保存下来的上下⽂，会存储在系统内核中，并 在任务重新调度执⾏时再次加载进来。这样就能保证任务原来的状态不受影响，让任务看起来 还是连续运⾏。
- 进程的上下⽂切换：保存内核态资源（当前进程的内核状态和 CPU 寄存器）之前，需要先把 该进程的⽤户态资源（虚拟内存、栈等）保存下来；⽽加载了下⼀进程的内核态后，还需要刷 新进程的虚拟内存和⽤户栈。
- 与线程相⽐，多进程的优势
  1. 资源保护⻆度：线程共享本进程的资源如内存、I/O、cpu等，不利于资源的管理和保护，⽽ 进程之间的资源是独⽴的，能很好的进⾏资源管理和保护。
  2. 程序执⾏⻆度：每个独⽴的进程有⼀个程序运⾏的⼊⼝，但是线程不能独⽴执⾏，必须依存 在应⽤程序中。
  3. 程序健壮性⻆度：多进程要⽐多线程健壮，⼀个进程崩溃后，在保护模式下不会对其他进程 产⽣影响，但是⼀个线程崩溃整个进程都死掉。



#### 进程通信方式（进程同步⽅式）

- 管道：
  1. 无名管道（内存文件）：管道是一种半双工的通信方式，数据只能单向流动，而且只能在具 有亲缘关系的进程之间使用。进程的亲缘关系通常是指父子进程关系。
  2. 有名管道（FIFO文件，借助文件系统）：有名管道也是半双工的通信方式，但是允许在没有 亲缘关系的进程之间使用，管道是先进先出的通信方式。
- 共享内存：共享内存就是映射一段能被其他进程所访问的内存，这段共享内存由一个进程创建，但 多个进程都可以访问。共享内存是最快的IPC方式，它是针对其他进程间通信方式运行效率低而专  门设计的。它往往与信号量，配合使用来实现进程间的同步和通信。`shm_open`和`ftruncate`和`mmap`
- 消息队列：消息队列是有消息的链表，存放在内核中并由消息队列标识符标识。消息队列克服了信 号传递信息少、管道只能承载无格式字节流以及缓冲区大小受限等缺点。存放在内核区域（0xC......以上）内。`msgget`和`msgrcv`和`msgsnd`和`msgctl`
- 套接字：适用于不同机器间进程通信，在本地也可作为两个进程通信的方式。
- 信号：用于通知接收进程某个事件已经发生，比如按下ctrl + C就是信号。
- 信号量：信号量是一个计数器，可以用来控制多个进程对共享资源的访问。它常作为一种锁机制， 实现进程、线程的对临界区的同步及互斥访问。`sem_wait`和`sem_post`



### 线程

#### 定义

- 是进程的⼀个执⾏单元，是进程内科调度实体。⽐进程更⼩的独⽴运⾏的基本单位。线程也被 称为轻量级进程。
-  ⼀个进程可以拥有多个线程，每个线程共享该线程的堆区域和数据区
- 线程的上下⽂切换：当进程只有⼀个线程时，可以认为进程就等于线程。 当进程拥有多个线 程时，这些线程会共享相同的虚拟内存和全局变量等资源。这些资源在上下⽂切换时是不需要 修改的。 另外，线程也有⾃⼰的私有数据，⽐如栈和寄存器等，这些在上下⽂切换时也是需 要保存的。
- 与进程相⽐，线程的优势
  1. 通信⻆度：进程间的通信必须请求操作系统服务（因为 CPU 要切换到内核态），开销很 ⼤。⽽同进程下的线程间通信，⽆需操作系统⼲预，开销更⼩。
  2. 程序执⾏⻆度：进程有顺序执⾏序列和程序⼊⼝，执⾏开销⼤；线程开销⼩。
  3. 上下⽂切换⻆度：线程会共享相同的虚拟内存和全局变量等资源。这些资源在上下⽂切换时 是不需要修改的，切换开销⼩。



#### 线程同步⽅式

互斥量、信号量、事件对象、临界区



#### linux实现

- Linux通过特殊的文件系统结构，如`/proc`文件系统，来管理和提供对进程和线程的访问，使得用户和程序可以通过文件系统的方式与这些系统实体进行交互。
- `/proc`文件系统是一个虚拟的文件系统，它不存储数据，而是提供了一种查看内核信息和改变内核设置的接口。在`/proc`目录下，每个正在运行的进程都有一个其进程ID命名的目录，例如`/proc/1234`，其中包含了与该进程相关的信息和配置。
- 在每个进程的`/proc/[pid]`目录下，可以找到多个文件和目录，这些文件和目录提供了关于进程状态的详细信息，包括：
  1. **`cmdline`**：显示启动进程的命令行参数。
  2. **`environ`**：包含进程的环境变量。
  3. **`status`**：显示进程的状态，包括其运行状态、内存使用、任务组信息等。
  4. **`fd`**：这是一个目录，包含了指向进程打开的文件描述符的符号链接。



### 协程

#### 定义

- 协程， ⼜称为微线程，拥有⾃⼰的上下⽂寄存器和栈。它不像线程和进程那样，需要进⾏系统内核上的上下⽂切换，协程的上下⽂切换是由开发⼈员决定的。
- 与线程相⽐，协程的优势
  1. ⽆需系统内核的上下⽂切换，减⼩开销；
  2. ⽆需原⼦操作锁定及同步的开销，不⽤担⼼资源共享的问题；
  3. 单线程即可实现⾼并发；
- 与线程相⽐，协程的缺点
  1. ⽆法使⽤ CPU 的多核
  2. 处处都要使⽤⾮阻塞代码



## 虚拟内存

虚拟内存可以让程序可以拥有超过系统物理内存大小的可用内存空间，虚拟内存为每个进程提供了一个一致的、私有的地址空间，它让每个进程产生了一种自己在独享主存的错觉

使用虚拟寻址，CPU需要将虚拟地址翻译成物理地址，这样才能访问到真实的物理内存。

这些页被映射到物理内存，但不需要映射到连续的物理内存，也不需要所有页都必须在物理内存中。当 程序引用到不在物理内存中的页时，由硬件执行必要的映射，将缺失的部分装入物理内存并重新执行失败的指令。

[详细介绍](https://www.xiaolincoding.com/os/3_memory/vmem.html#%E5%86%85%E5%AD%98%E5%88%86%E9%A1%B5)

### 内存分段

**window**

堆区、栈区、全局区、常量区、代码区

**linux**

stack、mmap（大块内存）、heap（小块内存）、bbs(未初始化的静态变量和全局变量)、data(初始化的静态变量和全局变量)、text、rodata(常量)



### 内存分页

#### 定义

**分页是把整个虚拟和物理内存空间切成一段段固定尺寸的大小**。这样一个连续并且尺寸固定的内存空间，我们叫**页**（*Page*）。在 Linux 下，每一页的大小为 `4KB`。

页表是存储在内存里的，**内存管理单元** （*MMU*）就做将虚拟内存地址转换成物理地址的工作。

而当进程访问的虚拟地址在页表中查不到时，系统会产生一个**缺页异常**，进入系统内核空间分配物理内存、更新进程页表，最后再返回用户空间，恢复进程的运行。

#### 页面置换算法

最佳置换法(OPT)、先进先出置换算法(FIFO)、最近最久未使用置换算法(LRU)、时钟置换算法(CLOCK)、改进型的时钟置换算法



#### malloc分配



##### brk与mmap

如果用户分配的内存小于 128 KB，brk() 系统调用从堆分配内存，通过 brk() 函数将「堆顶」指针向高地址移动，获得新的内存空间。

如果用户分配的内存大于 128 KB，mmap() 系统调用中「私有匿名映射」的方式，在文件映射区域分配内存，也就是从文件映射区“偷”了一块内存。

malloc() 分配的是虚拟内存，如果分配后的虚拟内存没有被访问的话，虚拟内存是不会映射到物理内存的，这样就不会占用物理内存了。

malloc() 在分配内存的时候，并不是老老实实按用户预期申请的字节数来分配内存空间大小，而是会**预分配更大的空间（预分配 132K 字节的内存）作为内存池**。



##### 分配流程

主线程的arnea称为“main_arena”。子线程的arnea称为“thread_arena”；主线程无论一开始malloc多少空间，只要size<128KB，kernel都会给132KB的heap segment(rw)。这部分称为main arena。 main_arena 并不在申请的 heap 中，而是一个全局变量，在 libc.so 的数据段。

1. 在第一次malloc的时候，glibc就会将堆切成两块chunk，第一块chunk就是分配出去的chunk，剩下的空间视为top chunk
2. 之后要是分配空间不足时将会由top chunk分配出去，它的size为表示top chunk还剩多少空间

假设 Top chunk 当前大小为 N 字节，用户申请了 K 字节的内存，那么 Top chunk 将被切割为：

- 一个 K 字节的 chunk，分配给用户
- 一个 N-K 字节的 chunk，称为 Last Remainder chunk

后者成为新的 Top chunk。如果连 Top chunk 都不够用了，那么：

- 在 main_arena 中，用 brk() 扩张 Top chunk
- 在 non_main_arena 中，用 mmap() 分配新的堆



##### 频繁mmap分配问题

**频繁通过 mmap 分配的内存话，不仅每次都会发生运行态的切换，还会发生缺页中断（在第一次访问虚拟地址后），这样会导致 CPU 消耗较大。**

为了改进这两个问题，malloc 通过 brk() 系统调用在堆空间申请内存的时候，由于堆空间是连续的，所以直接预分配更大的内存来作为内存池，当内存释放的时候，就缓存在内存池中。

等下次在申请内存的时候，就直接从内存池取出对应的内存块就行了，而且可能这个内存块的虚拟地址与物理地址的映射关系还存在，这样不仅减少了系统调用的次数，也减少了缺页中断的次数，这将大大降低 CPU 的消耗。



##### 全部使用brk分配的问题

如果我们连续申请了 10k，20k，30k 这三片内存，如果 10k 和 20k 这两片释放了，变为了空闲内存空间，如果下次申请的内存小于 30k，那么就可以重用这个空闲内存空间。

但是如果下次申请的内存大于 30k，没有可用的空闲内存空间，必须向 OS 申请，实际使用内存继续增大。

因此，随着系统频繁地 malloc 和 free ，尤其对于小块内存，堆内将产生越来越多不可用的碎片，导致“内存泄露”。而这种“泄露”现象使用 valgrind 是无法检测出来的。

所以，malloc 实现中，充分考虑了 brk 和 mmap 行为上的差异及优缺点，默认分配大块内存 (128KB) 才使用 mmap 分配内存空间。



#### 内存池

Bins为一个**单向或者双向链表**，存放着**空闲的chunk**（freed chunk）。glibc为了让malloc可以更快找到合适大小的chunk，因此在free掉一个chunk时，**会把该chunk根据大小加入合适的bin**中。

内存池分块由一个**malloc_chunk*类型的Bins数组**存储；

chunk最小为（size_t * 4），size_t = unsigned long int。Chunk由两部分组成，头部header（pre_size+size）和数据部分user data。如果该chunk被free，就会将chunk加入到名为bin的malloc_chunk*类型linked list。

按使用状态通常可分为**allocated chunk、free chunk、 top chunk 和last remainder chunk四种**。按大小可分为**fast、small、large和tcache chunk**

##### fast bin

单链表，存储在**类型为malloc_state的fastbinsY链表**里，无论多少位平台都为10个，但是使用多少个跟global_max_fast变量的值有关，默认7个。

默认情况下，对于 SIZE_SZ 为 4B 的平台， 小于 64B 的 chunk 分配请求，对于 SIZE_SZ 为 8B 的平台，小于 128B 的 chunk 分配请求，首先会查找fast bins中是否有所需大小的chunk存在（精确匹配），如果存在，就直接返回。

Fast bin的chunk 大小（含 chunk 头部）：0x10-0x40（64 位 0x20-0x80）B，相邻 bin 存放的大小相差 0x8（0x10）B。

总结以下特点：

- fastbinsY[]，fast bin存放在此数组中；
- 单向链表；
- LIFO（last in first out，后入先出，当下次malloc大小与这次free大小相同时，会从相同的bin取出，也就是会取到相同位置的chunk）；
- 管理 16、24、32、40、48、56、64 Bytes 的 free chunks（默认7个），最多加上 72、80 Bytes；
- 其中的chunk的in_use位（下一个物理相邻的chunk的P位）总为1。也就是说，释放到fastbin的chunk不会被清除in_use标志位。



##### small bin

双向链表，62个small bins，存储在bins[2]至bins[63]，同一个 small bin 链表中的 chunk 的大小相同。两个相邻索引的 small bin 链表中的 chunk 大小相差的字节数为 2 个机器字长，即 32 位相差 8 字节（4字节`fd` + 4字节`bk`），64 位相差 16 字节（8字节`fd` + 8字节`bk`）。

chunk size小于0x200（64位下0x400）字节的chunk叫做small chunk。

Chunk大小同样是从16字节（32字节）开始每次+8字节（16字节），最大16 + 8 * 61 = 504字节（32 + 16 * 61 = 1008字节）。

chunk size小于0x200（64位下0x400）字节（512或1024）的chunk叫做small chunk，而small bins存放的就是这些small chunk。Chunk大小同样是从16字节开始每次+8字节。

释放非 fast chunk 时，按以下步骤执行：

1. 若前一个相邻chunk空闲，则合并，触发对前一个相邻 chunk的unlink操作
2. 若下一个相邻chunk是top chunk，则合并并结束；否则继续执行 3
3. 若下一个相邻 chunk 空闲，则合并，触发对下一个相邻chunk的unlink 操作；否则，设置下一个相邻 chunk 的 PREV_INUSE 为 0
4. 将现在的chunk插入unsorted bin。
5. 若size超过了FASTBIN_CONSOLIDATION_THRESHOLD，则尽可能地合并 fastbin中的chunk，放入unsorted bin。若top chunk大小超过了 mp_.trim_threshold，则归还部分内存给 OS。

总结有以下特点。

- 双向循环链表
- Chunk size < 0x400 byte（64位）
- FIFO（先入先出）
- 根据大小再分成62个大小不同的bin
  - 0x20,0x30…0x60,0x70…



##### large bin

双向链表，63个large bins，存储在bins[64]至bins[126]，large bins 中的每一个 bin 都包含一定范围内的 chunk，其中的 chunk 按 fd 指针的顺序从大到小排列。相同大小的 chunk 同样按照最近使用顺序排列。

Large bins存放的是大于等于0x200（64位下0x400）字节的chunk。

bins[64]至bins[126]存放的字节大小递增：

- 前32个bins：从128 * SIZE_SZ 字节开始每次 + 16 * SIZE_SZ 字节
- 接下来的16个bins：每次+ 32 * SIZE_SZ 字节
- 接下来的8个bins：每次+ 64 * SIZE_SZ 字节
- 接下来的4个bins：每次 + 128 * SIZE_SZ 字节
- 接下来的2个bins：每次 + 256 * SIZE_SZ 字节
- 最后的1个bin：只有一个chunk，大小和large bins剩余的大小相同

同一个bin中的chunks不是相同大小的，按大小降序排列。

总结：

- 双向循环链表（每个节点降序排序）
- Chunk size >= 0x400
- Freed chunk多两个指针fd_nextsize、bk_nextsize指向前一块和后一块large chunk
- 根据大小再分成63个bin但大小不再是固定大小增加
  - 前32个bin为0x400+0x40*i
  - 32~48bin为0x1380+0x200*i
  - …以此类推
- 不再是每个bin中的chunk大小都固定，每个bin中存着该范围内不同大小的bin并在过程中进行排序用来加快寻找的速度，大的chunk会放在前面，小的chunk会放在后面
- FIFO（先入先出）



##### unsorted bin

双向链表，1个unsorted bin，存储在bins[1]，这里面的 chunk 没有进行排序，存储的 chunk 比较杂。

大小超过fast bins阈值（global_max_fast）的chunk被释放时会加入到unsorted bin，这使得ptmalloc2可以复用最近释放的chunk，从而提升效率。

分配时，如果在 unsorted bin 中没有合适的 chunk，就会把 unsorted bin 中的所有 chunk分别加入到所属的 bin 中，然后再在 bin 中分配合适的 chunk。

当程序申请大于global_max_fast内存时，glibc会遍历unsorted bin，每次取最后的一个unsorted bin。

1. 如果 unsorted chunk 满足以下四个条件，它就会被切割为一块满足申请大小的 chunk 和另一块剩下的 chunk，前者返回给程序，后者重新回到 unsorted bin。
   - 申请大小属于 small bin 范围
   - unosrted bin 中只有该 chunk
   - 这个 chunk 同样也是 last remainder chunk（N-K 字节的 chunk）
   - 切割之后的大小依然可以作为一个 chunk
2. 否则，从 unsorted bin 中删除 unsorted chunk。
   - 若 unsorted chunk 恰好和申请大小相同，则直接返回这个 chunk
   - 若 unsorted chunk 属于 small bin 范围，插入到相应 small bin
   - 若 unsorted chunk 属于 large bin 范围，则跳转到 3
3. 此时 unsorted chunk 属于 large bin 范围
   - 若对应 large bin 为空，直接插入 unsorted chunk，其 fd_nextsize 与 bk_nextsize 指向自身。
   - 否则，跳转到 4
4. 到这一步，我们需按大小降序插入对应 large bin
   - 若对应 large bin 最后一个 chunk 大于 unsorted chunk，则插入到最后
   - 否则，从对应 large bin 第一个 chunk 开始，沿 fd_nextsize（即变小）方向遍历，直到找到一个 chunk 命名为c，其大小小于等于 unsorted chunk 的大小
   - 若c大小等于unsorted chunk大小，则插入到c后面
   - 否则，插入到c前面

直到找到满足要求的unsorted chunk，或无法找到，去top chunk切割为止。总结以下特点。

- 双向循环链表
- 当free的chunk大小大于等于144字节时，为了效率，glibc并不会马上将chunk放到相对应的bin中，而会先放到unsorted bin
- 而下次malloc时将会先找找看unsorted bin中是否有合适的chunk，找不到才会去对应的bin中寻找，此时会顺便把unsorted bin的chunk放到对应的bin中，但small bin除外，为了效率，反而先从small bin找



##### Tcache

`glibc`从2.26版本开始引入的一个内存分配优化机制，类似于`fastbin`一样的东西，每条链上**最多可以有7个chunk**，`free`的时候当`tcache`满了才放入`fastbin`或`unsorted bin`，`malloc`的时候优先去`tcache`找。

`tcache`管理的内存块大小范围通常是小于或等于 512 字节（1024 字节）

`tcache`中存在多个`bin`（类似于`fast bin`和`small bin`），每个`bin`用于存储特定大小范围的内存块。`tcache` bin的大小是以`8`字节递增的，从`8`字节（即第一个`bin`）开始到最大允许大小（例如1024字节）。

例如，在64位系统中：

- `tcache` bin 1：存储大小为`8`字节的内存块。
- `tcache` bin 2：存储大小为`16`字节的内存块。
- `tcache` bin 3：存储大小为`24`字节的内存块。
- 依此类推，直到`128`个`bin`，用于存储最大`1024`字节大小的内存块。

基本工作方式：

- malloc 时，会先 malloc 一块内存用来存放 tcache_perthread_struct 。
- free 内存，且 size 小于 small bin size 时
  - 先放到对应的 tcache 中，直到 tcache 被填满（默认是 7 个）
  - tcache 被填满之后，再次 free 的内存和之前一样被放到 fastbin 或者 unsorted bin 中
  - tcache 中的 chunk 不会合并（不取消 inuse bit）
- malloc 内存，且 size 在 tcache 范围内
  - 先从 tcache 取 chunk，直到 tcache 为空
  - tcache 为空后，从 bin 中找
  - tcache 为空时，如果 fastbin/smallbin/unsorted bin 中有 size 符合的 chunk，会先把 fastbin/smallbin/unsorted bin 中的 chunk 放到 tcache 中，直到填满。之后再从 tcache 中取；因此 chunk 在 bin 中和 tcache 中的顺序会反过来。

**块释放策略**：

- 当释放一个内存块时，如果其大小小于或等于`tcache`的最大块大小，它将首先被放入`tcache`中。如果`tcache`已满（默认每个`bin`最多缓存`7`个块），块将被放入`fast bin`、`small bin`或`unsorted bin`中，具体取决于块的大小和其他因素。



#### free释放

malloc 通过 **brk()** 方式申请的内存，free 释放内存的时候，**并不会把内存归还给操作系统，而是缓存在 malloc 的内存池中，待下次使用**；

malloc 通过 **mmap()** 方式申请的内存，free 释放内存的时候，**会把内存归还给操作系统，内存得到真正的释放**。



## 网络传输协议

### TCP

1. TCP面向连接（如打电话要先拨号建立连接）
2. TCP提供可靠的服务。也就是说，通过TCP连接传送的数据，无差错，不丢失，不重复，且按序到 达
3. TCP面向字节流，实际上是TCP把数据看成一连串无结构的字节流
4. TCP有拥塞控制
5. 每一条TCP连接只能是点到点的
6. TCP首部开销20字节
7. TCP的逻辑通信信道是全双工的可靠信道
8. TCP是面向字节流的，它把上面应用层交下来的数据看成无结构的字节流会发送，可以想象成流水形式  的，发送方TCP会将数据放入“蓄水池”（缓存区），等到可以发送的时候就发送，不能发送就等着TCP会 根据当前网络的拥塞状态来确定每个报文段的大小。



#### TCP保活机制

如果两端的 TCP 连接一直没有数据交互，达到了触发 TCP 保活机制的条件，那么内核里的 TCP 协议栈就会发送探测报文。

- 如果**对端程序是正常工作**的。当 TCP 保活的探测报文发送给对端, 对端会正常响应，这样 **TCP 保活时间会被重置**，等待下一个 TCP 保活时间的到来。
- 如果**对端主机崩溃，或对端由于其他原因导致报文不可达**。当 TCP 保活的探测报文发送给对端后，石沉大海，没有响应，连续几次，达到保活探测次数后，**TCP 会报告该 TCP 连接已经死亡**。

所以，TCP 保活机制可以在双方没有数据交互的情况，通过探测报文，来确定对方的 TCP 连接是否存活。

在Linux系统中，最少需要经过 2 小时 11 分 15 秒才可以发现一个“死亡”连接。这个时间是怎么计算出来的呢？其实是通过 2 小时，加上 75 秒乘以 9 的总和。实际上，对很多对时延要求敏感的系统中，这个时间间隔是不可接受的。

没有开启 TCP keepalive，且双方也没有数据交互的情况下，如果**其中一方的进程发生了崩溃**，这个过程操作系统是可以感知的到的，于是就会**发送 FIN 报文给对方，然后与对方进行 TCP 四次挥手**。

- 在客户端主机宕机后，服务端向客户端发送的报文会得不到任何的响应，在一定时长后，服务端就会触发**超时重传**机制，重传未得到响应的报文。**客户端收到之前 TCP 连接的报文，会回复 RST 报文，以断开连接。**
- **超时重传**机制的tcp_retries2 设置了重传 15 次，并不代表 TCP 超时重传了 15 次才会通知应用程序终止该 TCP 连接，内核还会基于**「最大超时时间」**来判定。
- 每一轮的超时时间都是**倍数增长**的，比如第一次触发超时重传是在 2s 后，第二次则是在 4s 后，第三次则是 8s 后，以此类推。
- **在重传报文且一直没有收到对方响应的情况时，先达到「最大重传次数」或者「最大超时时间」这两个的其中一个条件后，就会停止重传**。



#### backlog

[backlog解析](https://blog.csdn.net/ywd171/article/details/137563880?spm=1001.2014.3001.5502)



#### 四元组

[四元组解析](https://blog.csdn.net/ywd171/article/details/130376668)



TCP socket运⾏过程 

1)⾸先服务端创建⼀个sockSrv，调⽤bind函数设定本地地址为addrSrv，之后调⽤listen函数将其设置为 LISTEN状态并创建连接队列，进⼊accpet函数的阻塞；

2)客户端创建⼀个sockCli，调⽤connect函数⾃动⽣成本地地址为addrCli，之后将其状态改为 CONNECTING，并将⼀个SYN包送⼊连接队列； 

3)服务端accpet函数检测到连接队列不为空，停⽌堵塞并⽣成⼀个sockCon，状态为ESTABLISHED，本 地地址为addrSrv，远程地址为addrCli，之后进⼊recv(sockCon)的阻塞； 

4)客户端检测到连接成功，将sockCli的状态从CONNECTING改为ESTABLISHED，远程地址设置为 addrSrv调⽤send(sockCli)向服务端发送信息,并进⼊recv(sockCli)的阻塞； 

5)服务端检测到消息，recv(sockCon)停⽌堵塞，接收消息并处理。之后调⽤send(sockCon)发送消息给 客户端，最后摧毁sockCon等待下⼀个客户端；

 6)客户端检测到消息，recv(sockCli)停⽌堵塞，接收消息并处理。之后摧毁sockCli(如果想再次通信，必 须配合服务器的⽅式，重新创建sockCli尝试再次连接)；



### UDP

1. UDP是无连接的，即发送数据之前不需要建立连接
2. UDP尽最大努力交付，即不保证可靠交付
3. UDP是面向报文的
4. UDP没有拥塞控制，因此网络出现拥塞不会使源主机的发送速率降低（对实时应用很有用，如IP电话， 实时视频会议等）
5. UDP的首部开销小，只有8个字节
6. UDP则是不可靠信道
7. UDP是面向报文的，发送方的UDP对应用层交下来的报文，不合并，不拆分，只是在其上面加上首 部后就交给了下面的网络层，论应用层交给UDP多长的报文，它统统发送，一次发送一个。而对接收  方，接到后直接去除首部，交给上面的应用层就完成任务了。因此，它需要应用层控制报文的大小



### HTTP

HTTP协议传输的数据都是未加密的，也就是明文的，因此使用HTTP协议传输隐私信息非常不安全

http用的端口是80



### HTTPS

HTTPS协议是由SSL+HTTP协议构建的可进行加密传输、身份认证的网络协议，要比http协议安全。

让 **HTTP 先和 SSL（Secure Sockets Layer）通信，再由 SSL 和 TCP 通信，也就是说 HTTPS 使用了隧道进行通信**。

https协议需要到ca申请证书，一般免费证书较少，因而需要一定费用。

https用的端口是443



## 排序

### 快排

```c++
class Solution {
public:
    void quickSort(vector<int>& nums, int left, int right){
        if(left >= right) return;
        int i = left, j = right, tmp = nums[left];
        while(i < j){
            while(i < j && nums[j] >= tmp) --j;// 一定要右边先遍历
            while(i < j && nums[i] <= tmp) ++i;
            if(i < j) swap(nums[i], nums[j]);
        }
        swap(nums[i], nums[left]);
        quickSort(nums, left, i - 1);
        quickSort(nums, i + 1, right);
    }
    vector<int> sortArray(vector<int>& nums) {
        quickSort(nums, 0, nums.size() - 1);
        return nums;
    }
};
```

**优化：**

1. 随机选取法
2. 快速排序算法的每个递归子数组排序前进行有序性检查
3. 三数取中法
4. 结合直接插入排序
5. 多线程优化



## 流媒体

### RTSP 实时传输协议

- RTSP 协议是基于 RTP 和 RTCP 之上的 
- RTSP **使用 RTP 传输媒体数据**，**使用 RTCP 来交互控制命令** 
- 整个协议是**先用 RTSP 进行信息交互** 
- 然后再**使用 RTP/RTCP 进行媒体数据传输**
- 当应用程序建立一个RTP会话时，应用程序将确定**一对目的传输地址**。目的传输地址由一个网络地址和一对端口组成，有两个端口：**一个给RTP包，一个给RTCP包**，使得RTP/RTCP数据能够正确发送。**RTP数据发向偶数的UDP端口，而对应的控制信号RTCP数据发向相邻的奇数UDP端口（偶数的UDP端口＋1）**，这样就构成一个UDP端口对。

不使用RTSP做直播是因为浏览器不支持RTSP，且一些防火墙使用UDP打洞的话会失败，加上浏览器对RTMP支持

#### RTSP

RTSP (Real-Time Stream Protocol) 实时流传输协议是一种基于文本的应用层协议，常被用于 **建立的控制媒体流的传输**，该协议用于 **C/S 模型** , 是一个 **基于文本** 的协议，用于在客户端和服务器端建立和协商实时流会话。

信息交互阶段使用TCP，视频传输基于UDP

rtsp客户端的请求命令格式：

```
Method url version \r\n
CSeq : seq\r\n
... \r\n\r\n
```

- **Method ：**表示本次命令的含义，⼀般可设置为： 

  - **OPTIONS ：** 请求是客户端向服务器询问可用的方法

  - **DESCRIBE ：** 客户端向服务器请求媒体资源描述，服务器端通过**SDP(Session Description Protocol)格式**回应客户端的请求。资源描述中会列出所请求媒体的媒体流及其相关信息，典型情况下，音频和视频分别作为一个媒体流传输。
  - **SETUP ：** 确定了具体的媒体流如何传输，该请求必须在PLAY请求之前发送。SETUP请求包含媒体流的URL和客户端用于接收RTP数据(audio or video)的端口以及接收RTCP数据(meta information)的端口。服务器端的回复通常包含客户端请求参数的确认，并会补充缺失的部分，比如服务器选择的发送端口。每一个媒体流在发送PLAY请求之前，都要首先通过SETUP请求来进行相应的配置。
  - **PLAY ：** 客户端通过PLAY请求来播放一个或全部媒体流，PLAY请求可以发送一次或多次，发送一次时，URL为包含所有媒体流的地址，发送多次时，每一次请求携带的URL只包含一个相应的媒体流。PLAY请求中可指定播放的range，若未指定，则从媒体流的开始播放到结束，如果媒体流在播放过程中被暂停，则可在暂停处重新启动流的播放。
  - **PAUSE：**请求会暂停一个或所有媒体流，后续可通过PLAY请求恢复播放。PAUSE请求中携带所请求媒体流的URL，若参数range存在，则指明在何处暂停，若该参数不存在，则暂停立即生效，且暂停时长不确定。
  - **TEARDOWN ：** 结束会话请求，该请求会停止所有媒体流，并释放服务器上的相关会话数据。
  - **GET_PARAMETER：**检索指定URI数据中的参数值。不携带消息体的GET_PARAMETER可用来测试服务器端或客户端是否可通(类似ping的功能)。
  - **SET_PARAMETER：**设置指定媒体流的参数。
  - **REDIRECT：**重定向请求，用于服务器通知客户端新的服务地址，客户端需要向这个新地址重新发起请求。重定向请求中可能包含Range参数，指明重定向生效的时间。客户端若需向新服务地址发起请求，必须先teardown当前会话，再向指定的新主机setup一个新的会话。
  - **ANNOUNCE：**两个用途：(1)C->S：客户端向服务器端发布URL指定的媒体信息描述；(2) S->C：实时更新对话描述。若媒体表示中新增了一个媒体流，例如在直播过程中，则整个媒体表示的description都要被重新发送，而不是只发送新增部分。
  - **RECORD：**请求录制指定范围的媒体数据，请求中可指定录制的起止时间戳；若未指定时间范围，则使用presentation description中的开始和结束时间，这种情况下，如果会话已开始，则立即启动录制操作。

- **url ：**请求地址，⼀般为rtsp://ip:port/session 默认端⼝为554或8554 

- **version ：**协议版本，⼀般取RTSP/1.0 

- **CSeq ：**序列号，递增的整数 

- ... ：其他信息



**SDP**

SDP(SessionDescription Protocol )会话描述协议，用于描述多媒体会话，它为会话通知、会话初始和其它形式的多媒体会话初始等操作提供服务。

SDP的设计宗旨是通用性协议，所有它可以应用于很大范围的网络环境和应用程序，但 SDP 不支持会话内容或媒体编码的协商操作。

SDP信息包括：

- 会话名称和目标；
- 会话活动时间；
- 构成会话的媒体；
- 有关接收媒体的信息、地址等。



#### RTP

RTP (Real-time Transport Potocol) 实时传输协议，用于 **实时数据的传输**，建立在UDP上。

- **版本号（V）：**2比特，用来标志使用的RTP版本。
- **填充位（P）：**1比特，如果该位置位，则该RTP包的尾部就包含附加的填充字节。
- **扩展位（X）：**1比特，如果该位置位的话，RTP固定头部后面就跟有一个扩展头部。
- **CSRC计数器（CC）：**4比特，含有固定头部后面跟着的CSRC的数目。
- **标记位（M）：**1比特,该位的解释由配置文档（Profile）来承担.
- **载荷类型（PT）：**7比特，标识了RTP载荷的类型。
- **序列号（SN）：**16比特，发送方在每发送完一个RTP包后就将该域的值增加1，接收方可以由该域检测包的丢失及恢复包序列。序列号的初始值是随机的。
- **时间戳：**32比特，记录了该包中数据的第一个字节的采样时刻。在一次会话开始时，时间戳初始化成一个初始值。即使在没有信号发送时，时间戳的数值也要随时间而不断地增加（时间在流逝嘛）。时间戳是去除抖动和实现同步不可缺少的。
- **同步源标识符(SSRC)：**32比特，同步源就是指RTP包流的来源。在同一个RTP会话中不能有两个相同的SSRC值。该标识符是随机选取的 RFC1889推荐了MD5随机算法。
- **贡献源列表（CSRC List）：**0～15项，每项32比特，用来标志对一个RTP混合器产生的新包有贡献的所有RTP包的源。由混合器将这些有贡献的SSRC标识符插入表中。SSRC标识符都被列出来，以便接收端能正确指出交谈双方的身份。



#### RTCP

RTCP (Real-time Transport Control Protocol) 实时传输控制协议， `RTCP` 为 `RTP` 数据流提供 **信道外控制**，RTCP 的主要功能是保证服务质量，**为 RTP 提供服务质量反馈**，建立在UDP上。

**RTP只负责传输数据包，**需要与RTCP配合使用，**由RTCP来保证RTP数据包的服务质量**。RTCP的主要功能：服务质量的监控和反馈、媒体设备间的同步以及多播组中的成员标识。在RTP会话期间，各参与者周期性传送RTCP数据包，RTCP数据包中包含已发送的数据包数量、丢失的数据包数量等信息，各参与者通过这些信息动态改变传输速率或传输的数据类型。

假如视频数据从A发送到B，因此我们需要建立2个socket，一个负责从A发送RTP视频数据包到B，一个负责收发RTCP数据包。**通过RTCP控制包中的信息判断网络状况更改码率适应网络带宽**。A端周期性发送SR--发送者报告包给B端，B端周期性回复SR包，往A端发送RR--接收者报告包，告知A端接收状况，这样A端可以**估算出现在的网络状况，调整A端发送速度，改善视频质量**。具体的调整算法要经过网络测试获得。不是固定不变的。

上面说的是单播情况下RTP与RTCP的配合，如果建立在多播情况下，如多方通话，视频会议等的时候，RTCP的作用更加大，因为RTCP中的源描述包，就是各个与话人存在的证明，一个身份的标识，加入通话，则需要周期性发送SDES源描述包，退出通话需要发送BYE包，删除相应的SDES，其次RTCP的发送周期会随着人数的增减而动态变化,以改善通话质量。

**报文种类：**

- **SR:** 发送者报告, type-200
- **RR:** 接受者报告, type-201
- **SDES:** source description 源信息报告, type-202
- **BYE:** 成员管理报告, type-203
- **APP:** 自定义报告, type-204



### RTMP

RTMP协议是应⽤层协议，是要靠底层可靠的传输层协议（通常是TCP）来保证信息传输的可靠性的。在 基于传输层协议的链接建⽴完成后，RTMP协议也要客户端和服务器通过“握⼿”来建⽴基于传输层链接之 上的RTMP Connection链接，在**Connection链接上会传输⼀些控制信息**，如 SetChunkSize,SetACKWindowSize。其中**CreateStream命令会创建⼀个Stream链接**，⽤于传输具体的 ⾳视频数据和控制这些信息传输的命令信息。RTMP协议传输时会对数据做⾃⼰的格式化，这种格式的消 息我们称之为**RTMP Message**，⽽实际传输的时候为了更好地实现多路复⽤、分包和信息的公平性，发送 端会把**Message划分为带有Message ID的Chunk**，每个Chunk可**能是⼀个单独的Message，也可能是 Message的⼀部分**，在接受端会根据chunk中包含的data的⻓度，message id和message的⻓度把 chunk还原成完整的Message，从⽽实现信息的收发。

[协议解析](https://www.cnblogs.com/Kingfans/p/7083100.html)

#### 握手

要建⽴⼀个有效的RTMP Connection链接，⾸先要“握⼿”:客户端要向服务器发送C0,C1,C2（按序）三个 chunk，服务器向客户端发送S0,S1,S2（按序）三个chunk，然后才能进⾏有效的信息传输。RTMP协议 本身并没有规定这6个Message的具体传输顺序，但RTMP协议的实现者需要保证这⼏点： 

- 客户端要等收到S1之后才能发送C2 
- 客户端要等收到S2之后才能发送其他信息（控制信息和真实⾳视频等数据） 
- 服务端要等到收到C0之后发送S1 
- 服务端必须等到收到C1之后才能发送S2 
- 服务端必须等到收到C2之后才能发送其他信息（控制信息和真实⾳视频等数据） 

C0和S0是双方RTMP的版本

C1和C2是相同的随机**1528字节**的数据，S1和S2同理

**状态：**

- **Uninitialized (未初始化)：**协议的版本号在这个阶段被发送。客户端和服务器都是 uninitialized (未初始化) 状态。之后客户端在数据包 C0 中将协议版本号发出。如果服务器支持这个版本，它将在回应中发送 S0 和 S1。如果不支持呢，服务器会才去适当的行为进行响应。在 RTMP 协议中，这个行为就是终止连接。
- **Version Sent (版本已发送)：**在未初始化状态之后，客户端和服务器都进入 Version Sent (版本已发送) 状态。客户端会等待接收数据包 S1 而服务器在等待 C1。一旦拿到期待的包，客户端会发送数据包 C2 而服务器发送数据包 S2。(客户端和服务器各自的)状态随即变为 Ack Sent (确认已发送)。
- **Ack Sent (确认已发送)：**客户端和服务器分别等待 S2 和 C2。
- **Handshake Done (握手结束)：**客户端和服务器可以开始交换消息了。



#### RTMP拉流流程

1. TCP三次握手
2. RTMP握手
3. RTMP建立连接
   1. 客户端发送命令消息中的“连接”(connect)到服务器，请求与一个服务应用实例建立连接。
   2. 服务器接收到连接命令消息后，发送**确认窗口大小**(Window Acknowledgement Size)协议消息到客户端，同时连接到连接命令中提到的应用程序。
   3. 服务器发送设置**带宽协议**(Set Peer Bandwidth)消息到客户端。
   4. 客户端处理设置带宽协议消息后，发送确认窗口大小(Window Acknowledgement Size)协议消息到服务器端
   5. 服务器发送用户控制消息中的“流开始”(Stream Begin)消息到客户端。
   6. 服务器发送命令消息中的“结果”(_result)，通知客户端连接的状态。
4. RTMP建流&Play
   1. 客户端发送建流命令(createstream)到服务器
   2. 客户端在收到来自服务器的建流命令(createstream)的成功结果(_result)后发送(play)命令。
   3. 服务器在接收到play命令后，**发送一个来设置块大小(SetChunkSize)消息**。
   4. 服务器发送另一个用户控制消息，指定事件“流记录”(StreamIsRecorded)和流ID。这个消息的头2字节携带事件类型，最后4字节携带流ID。
   5. 服务器发送另一个用户控制消息，指定事件“流开始”(StreamBegin)。向客户端指示流的开始。
   6. 如果客户端发送的播放(play)命令成功，服务器发送命令消息(onStatus)，NeStream.Play.Start & NeStream.Play.Reset。
   7. 只有当客户端发送的play命令设置了reset标志时，服务器才会发送NeStream.Play.Reset。
   8. 如果没有找到要播放的流，服务器将发送onStatus消息NeStream.Play.StreamNotFound。
   9. 之后，客户端播放服务器发送的音频和视频数据
   10. 一帧作为一个message发送
5. deleteStream删除流



#### RTMP Chunk Stream

Chunk Stream是对传输RTMP Chunk的流的逻辑上的抽象，客户端和服务器之间有关RTMP的信息都在 这个流上通信。这个流上的操作也是我们关注RTMP协议的重点。

- **Message：**满⾜该协议格式的、可以切分成Chunk发送的消息
- **Chunking：**RTMP在收发数据的时候并不是以Message为单位的，⽽是把Message拆分成Chunk发送，⽽且必须 在⼀个Chunk发送完成之后才能开始发送下⼀个Chunk。每个Chunk中带有MessageID代表属于哪个 Message，接受端也会按照这个id来将chunk组装成Message。
- **协议控制消息（Protocol Control Message）：**在RTMP的chunk流会⽤⼀些特殊的值来代表协议的控制消息，它们的Message Stream ID必须为 0（代表控制流信息），CSID必须为2，Message Type ID可以为1，2，3，5，6，具体代表的消息会在 下⾯依次说明。控制消息的接受端会忽略掉chunk中的时间戳，收到后⽴即⽣效。
  1. **Set Chunk Size(ID=1)：**设置chunk中Data字段所能承载的最⼤字节数，在实际写代码的时候⼀般会把chunk size设置的很⼤，有的会 设置为4096，FFMPEG推流的时候设置的是 60*1000，这样设置的好处是避免了频繁的拆包组包，占 ⽤过多的CPU。
  2. **Abort Message (ID=2)：**当⼀个Message被切分为多个chunk，接受端只接收到了部 分chunk时，发送该控制消息表示发送端不再传输同Message的chunk，接受端接收到这个消息后要丢 弃这些不完整的chunk。
  3. **Acknowledgement (ID=3)：**⽤于设置窗⼝确认⼤⼩
  4. **Window Acknowledgement Size (ID=5)：**窗⼝确认消息
  5. **Set Peer Bandwidth (ID=6)：**限制对端的输出带宽
- **不同类型的RTMP Message**
  - **Command Message(命令消息，Message Type ID＝17或20)：**表示在客户端盒服务器间传递的在对 端执⾏某些操作的命令消息，如connect表示连接对端，对端如果同意连接的话会记录发送端信息并返 回连接成功消息，publish表示开始向对⽅推流，接受端接到命令后准备好接受对端发送的流信息
  - **Data Message（数据消息，Message Type ID＝15或18）：**传递⼀些元数据（MetaData，⽐如视频 名，分辨率等等）或者⽤户⾃定义的⼀些消息。
  - **Shared Object Message(共享消息，Message Type ID＝16或19)：**表示⼀个Flash类型的对象，由 键值对的集合组成，⽤于多客户端，多实例时使⽤。
  - **Audio Message（⾳频信息，Message Type ID＝8）：**⾳频数据。 
  - **Video Message（视频信息，Message Type ID＝9）：**视频数据。 
  - **Aggregate Message (聚集信息，Message Type ID＝22)：**多个RTMP⼦消息的集合 
  - **User Control Message Events(⽤户控制消息，Message Type ID=4)：**告知对⽅执⾏该信息中包含的 ⽤户控制事件，⽐如Stream Begin事件告知对⽅流信息开始传输。和前⾯提到的协议控制信息 （Protocol Control Message）不同，这是在RTMP协议层的，⽽不是在RTMP chunk流协议层的， 这个很容易弄混。



### HLS 直播协议

HTTP Live Streaming（缩写是 HLS）是一个由苹果公司提出的基于 HTTP的流媒体网络传输协议。是苹果公司QuickTime X和iPhone软件系统的一部分。它的工作原理是**把整个流分成一个个小的基于HTTP的文件来下载，每次只下载一些**。当媒体流正在播放时，客户端可以选择从许多不同的备用源中以不同的速率下载同样的资源，允许流媒体会话适应不同的数据速率。

HLS协议主要有两种版本，mpegts版本和fmp4，顾名思义，他们主要的区别在于封装容器的不同，前者基于mpegts封装，mpegts因为有出色的纠错能力，常用于广播电视卫星传输。而后者在2017年8月正式进入ISO RFC8216标准，fMP4(分段式MP4)的封装格式正式被HLS支持。

fMP4不是一种新的封装格式，而是在传统MP4格式基础上增加若干新特性。传统的MP4因为文件结构所限，所有的索引和数据都是放在一起的，在播放时两者缺一不可，无法实现流式直播。fMP4主要是将MP4文件切割成更细的粒度，播放器可以加载一小部分数据之后就可以进行播放，这样就可以实现直播。

#### 服务器

- 服务器部分负责将输⼊的⾳视频媒体内容转换成为适合于内容分发组件进⾏递送的格式。
- 对于视频直播， 编码器⾸先将**摄像机实时采集的⾳视频数据压缩编码为符合特定标准的⾳视频基本流**(⽬前苹果的系统仅⽀ 持H.264视频和AAC⾳频)，然后再**复⽤和封装成为符合MPEG-2系统层标准的传输流(TS)格式**进⾏输出。 
- 流分割器(Stream Segmenter)负责将**编码器输出的MPEG-2 TS流分割为⼀系列连续的、⻓度均等的⼩ TS⽂件(后缀名为.ts)**，并依次发送⾄内容分发组件中的 Web服务器进⾏存储。
- 与此同时，为了跟踪播放过程中媒体⽂件的可⽤性和当前位置，流分割器还需**创建⼀个含有指向这些⼩TS⽂件指针的索引⽂件**，同样放置于Web服务器之中。
- 这个索引⽂件可以看作是**⼀个服务器连续媒体流中的播放列表滑动窗⼝**，每当流分割器**⽣成⼀个新的TS⽂件时，这个索引⽂件的内容也被更新，新的⽂件URI(统⼀资源定位符)加⼊到滑动窗⼝的末尾，⽼的⽂件URI则被移去**，这样索引⽂件中将始 终包含最新的固定数量的x个分段。
- 流分割器还可以对其**⽣成的每个⼩TS⽂件进⾏加密，并⽣ 成相应的密钥⽂件**。
- 之所以采⽤**MPEG-2 TS格式**来对编码后的媒体流进⾏统⼀封装，是因为它能够将**⾳视频媒体流严格按时序进⾏交织复⽤**，任意截取和分段后，每⼀个分段都**可能不依赖于之前的分段⽽独⽴进⾏解码和播放**。
- 为此，TS⽂件中必须仅包含⼀个MPEG-2节⽬，在每个⽂件的开头应**包含⼀个节⽬关联表(PAT)和⼀个节⽬ 映射表(PMT)**，包含视频的⽂件中还必须含有**⾄少⼀个关键帧和其他⾜够信息(如序列头)**⽤以完成解码器的 初始化。
- 索引⽂件采⽤扩展的M3U播放列表格式，**后缀名为.m3u8**。
- M3U播放列表是⼀个由**若⼲⽂本⾏组 成的⽂本⽂件**，其中每⼀⾏要么是⼀个URI，⼀个空⾏，或者是⼀个以注释符“#”起始的⾏。
- 每个URI⾏指 向⼀个分段的媒体⽂件，或者⼀个衍⽣的索引(播放列表)⽂件。
- 除了以**“#EXT”起始的⾏是标签⾏**外，其他 **以“#”起始的⾏是注释**，应予忽略。



#### 内容分发

- 内容分发系统⽤于通过**HTTP协议**将分割后的⼩媒体⽂件及其索引⽂件递送⾄客户端播放器，它既可以是⼀ 个普通的Web服务器，也可以是⼀个Web缓存系统。
- ⼏乎不需要对Web服务器做任何特殊的配置，以及增 加其他定制的模块。
- 推荐的配置仅限于**对.m3u8⽂件和.ts⽂件**的MIME类型关联。



#### 客户端

- 通常情况下，客户端软件通过**访问Web⽹⻚中的URL链接**来获取和下载⼀个流媒体会话的索引⽂件。
- 这个 索引⽂件进⼀步指定了服务器上当前可⽤的TS格式媒体⽂件、解密密钥和其他替换流的位置。
- 对于选定的 媒体流，客户端依次下载索引⽂件中列出的每⼀个可⽤媒体⽂件。
- 当这些媒体⽂件**缓冲够⼀定数量**后，客 户端将它们按**顺序重新拼装成⼀个连贯的TS流**，然后发送⾄播放器进⾏解码和呈现。
- 对于加密的媒体⽂件，客户端还负责**根据索引⽂件的指引来获取解密密钥**，提供⽤户认证接⼝，并按需进⾏解密。对于视频 点播，上述过程不断持续下去，直到客户端碰到**索引⽂件中的#EXT-X-ENDLIST标签**。
- 对于视频直播，**索引⽂件中不存在#EXT-X-ENDLIST标签**，客户端将**周期性地向Web服务器重新请求获取该索引⽂件的更新版本**，然后在这个更新版的索引⽂件中**查找新的媒体⽂件和解密密钥**，并将它们的 URI添加⾄下载队列的末尾。
- 需要注意HTTP Live Streaming并不是⼀个真正实时的流媒体系统，这是因为**对应于媒体分段的⼤⼩和持续时间有⼀定潜在的时间延迟**。
- 在客户端中，⾄少在**⼀个分段媒体⽂件被完全下载**之后才能够开始播放，⽽ 通常要求下载完成**两个分段媒体⽂件**之后才开始播放以保证不同分段⾳视频之间的⽆缝连接。
- 此外，在客户端开始下载之前，必须等待服务器端的编码器和流分割器⾄少⽣成⼀个TS⽂件，这也会带来潜在的时延。
- 在推荐配置下，HTTP Live Streaming系统的典型时延约在**30s**左右。



**优化**

1. 在HLS标准当中，**每一个单独的切片文件都是一个GOP**，一个TS文件推荐10s，但直播场景中用户推流的GOP大小都不固定，如果完全按照标准去实现的话就可能会导致切片长度过大，客户端下载时间过长，从而导致启播时间比较久。

2. 在我们的实践中，首先采用了**1s长度的切片**，可以尽可能缩短因为产生切片所造成延迟，这样会出现一个问题，就是每一个切片可能不是一个GOP， 如果按照默认的客户端拉播放列表中最后三个切片进行播放，一旦倒数第三个的第一帧不是关键帧就会导致启播很慢或无法启播。**在HLS标准中#EXT-X-START:TIME-OFFSET=0标签会告知客户端，从列表的第一个切片开始播放**。

3. 此时我们只需要保证**列表的第一个切片的第一帧为关键帧**，客户端就可以快速加载到关键帧。
4. 此外，利用了fmp4和CMAF(一种基于mp4上的封装技术，与fmp4非常类似)的封装特性，在一个切片文件中可以进**行了更小粒度的封装(chunk)**，使得客户端在下载几帧数据后就可以开始进行解码播放。如上图，对于第一个切片文件，客户端只需要下载第一个chunk拿到第一帧，就可以完成启播。这样就完成了不在破坏标准兼容性的基础上实现了启播的优化
5. 由于采用了1s一个切片的方式，相比较RTMP/FLV来讲，整体延迟只增加了1s，是一个可以接受的范围。相比较传统的HLS来讲，首帧提速2-3倍，配合客户端的追帧策略也可以使延迟得到很好的控制。



### HTTP-FLV

#### FLV

FLV(Flash Video)是Adobe公司推出的⼀种流媒体格式，由于其封装后的⾳视频⽂件体积 ⼩、封装简单等特点，⾮常适合于互联⽹上使⽤。⽬前主流的视频⽹站基本都⽀持FLV。采⽤ FLV格式封装的⽂件后缀为.flv。

FLV封装格式是由⼀个**⽂件头(file header)和 ⽂件体(file Body)**组成。其中，FLV body由⼀对对的**(Previous Tag Size字段 + tag)**组成。Previous Tag Size字段**排列在Tag之前**，占⽤ 4个字节。Previous Tag Size记录了**前⾯⼀个Tag的⼤⼩，⽤于逆向读取处理**。FLV header **后的第⼀个Pervious Tag Size的值为0**。

flv需要metadata数据才可以seek。

Tag⼀般可以分为3种类型：**脚本(帧)数据类型、音频数据类型、视频数据**。FLV数据以**⼤端序**进⾏存储，在解析时需要注意。

##### FLV header

- **签名：**⽆符号整形，'F'(0x46) 
- **签名：**⽆符号整形，L'(0x4C) 
- **签名：**⽆符号整形，'V'(0x56) 
- **版本：**⽆符号整形，FLV的版本。0x01表示FLV版本为1 
- **保留字段：**⼀个字节的1位，前五位都为0 
- **⾳频流标识：**⼀个字节的1位，是否存在⾳频流 
- **保留字段：**⼀个字节的1位，为0 
- **视频流标识：**⼀个字节的1位，是否存在视频流 
- **⽂件头⼤⼩：**四个字节，FLV版本1时填写9，表明的是FLV头的⼤⼩，为后期的 FLV版本扩展使⽤。包括这四个字节。数据的起始位置 就是从⽂件开头偏移这么多的⼤⼩。



##### FLV Body

FLV Header之后，就是FLV File Body。FLV File Body是由⼀连串的**back-pointers + tags**构成。 Back-pointer表示**Previous Tag Size(前⼀个tag的大小)，占4个字节**。



##### FLV Tag

每⼀个Tag也是由**两部分组成：tag header和tag data**。Tag Header⾥**存放的是当前tag的类型、数据区(tag data)的⻓度**等信息。

⼀个视频tag⼀般只包含⼀帧视频的码流 

其中视频tag的时间戳对应的是解码时间戳（DTS/90）



##### tag header

tag header⼀般占**11个字节**的内存空间。

- **Tag类型 Type** ：1字节 **8:audeo 9:video 18:Script data(脚本数据)** all Others:reserved 其他所有值未使⽤ 
- **数据区⼤⼩**： 3字节 当前tag的数据域的⼤⼩，不包含tag header。 Length of the data in the Data field 
- **时间戳 Timestamp**：3字节 当前帧时戳，单位是毫秒。相对值，第⼀个tag的时戳总是 为0 
- **时戳扩展字段 TimestampExtended**： 1字节 如果时戳⼤于0xFFFFFF，将会使⽤这个字节。这个字节是 时戳的⾼8位，上⾯的三个字节是低24位。 
- **StreamID**：3字节 总是为0

注意： 

1. **flv⽂件中Timestamp和TimestampExtended拼出来的是dts。也就是解码时间。** Timestamp和TimestampExtended拼出来dts单位为ms。(如果不存在B帧，当然dts等于 pts) 
2. CompositionTime 表示PTS相对于DTS的偏移值， 在每个视频tag的第14~16字节。 显示时间(pts) = 解码时间（tag的第5~8字节） + CompositionTime CompositionTime的单位也是ms



##### Script Tag Data结构(脚本类型、帧类型)

该类型Tag⼜被称为MetaData Tag,存放⼀些关于FLV视频和⾳频的元信息，⽐如：duration、width、 height等。通常该类型Tag会作为FLV⽂件的第⼀个tag，并且只有⼀个，跟在File Header后。



##### Video Tag Data结构(视频类型)

视频Tag Data开始的： 

- **第⼀个字节**包含视频数据的参数信息
- **第⼆个字节**开始为视频流数据。



##### Audio Tag Data结构(⾳频类型)

⾳频Tag Data区域开始的： 

- 第⼀个字节包含了⾳频数据的参数信息
- **第⼆个字节开始为⾳频流数据**。 

**（这两个字节属于tag的data部分，不是header部分）**



#### HTTP-FLV

##### 流形式

- **FLV文件，渐进式HTTP流**。放一个文件到nginx目录，可以访问下载在播放器播放，这是HTTP FLV文件，也就是渐进式下载流，其实是一个点播流。所谓渐进式下载，也就是用户观看时无法从未下载的地方开始看。
- **FLV伪流**。一般说的HTTP FLV，比上面的渐进式流高级一点，譬如，一个120分钟的电影，作为渐进式流播放时，用户需要从60分钟开始看，如何支持呢？因为nginx是当做文件下载的，无法直接跳转到第60分钟（nginx也不知道60分钟对应的字节偏移是多少呀）。后来有人就支持这种跳着播放，通过指定时间服务器从指定的位置开始给流，这种支持flv?start=，就是http flv的伪流，本质上还是点播流。
- **FLV直播流**，是严格意义上的直播流，有RTMP的所有特征，譬如集群、低延迟、热备、GOP cache，而且有HTTP的优势，譬如302、穿墙、通用。

##### 数据传输流程

1. 用户直播开始推rtmp流到服务器，服务器转换成HTTP-FLV流。

2. 观众（客户端）播放，先发送http get请求到服务器（请求的uri就是HTTP-FLV的播放地址，形如"http://ossrs.net:8081/live/livestream.flv"），请求HTTP-FLV流。

3. 服务器向客户端发送http响应和发送flv格式的数据。无论任何时刻进入直播间，服务器都会按照**flv封装格式的数据顺序发送数据**，即按照**flv header，previous tag size，metadata，aac/avc squence header，video/audio tag**这种顺序发送数据。在HTTP/1.1中，每个块的大小是可以根据具体情况来确定的，**没有固定的大小限制**。服务器可以根据**网络状况和传输效率**来决定每个块的大小。

4. 观众（客户端）侧收到数据后会进行组包操作，组成**一个个完整的flv tag**，然后进行播放。由于音视频数据是分块传输到客户端的，所以**一个数据包往往不是一个完整的flv tag，有可能是tag的一部分，也有可能是多个tag**。当不足一个tag的时候，客户端需要进行**组包，即将几个数据包中的数据组成一个完整的tag**，然后再进行播放或其它操作。

5. 断开连接，用户直播结束，服务器主动断开连接 或 观众退出直播间，客户端主动断开连接。
   



### GB28181

分为SIP模块以及流媒体模块

**信令沟通流程**

1. 摄像头向服务器发送**REGISTER**，注册信息，其中sip号是服务器信息，格式是`SIP服务器编号@SIP服务器地址:SIP服务器端口`
2. SIP服务器回复一个401，表示未授权，未携带密码
3. 摄像头第二次发送**REGISTER**，并且携带密钥
4. SIP服务器返回200，表示注册成功
5. SIP服务器向摄像头发送**INVITE**，告诉摄像头需要接收流媒体，其中sip号是摄像头信息，格式是`摄像头编号@摄像头地址:摄像头端口`
6. 摄像头回复100，表示尝试中
7. 摄像头会隔一段时间给SIP服务器发送保活报文**MESSAGE**
8. 摄像头回复200，表示成功，其中报文里包含SDP的信息（媒体流描述：视频流，视频流推流到服务器的端口，视频流状态（仅发送））
9. SIP服务器向摄像头发送ACK，表示确认收到，且准备好了UDP端口接收数据




### m3u8

M3U8视频格式是一种基于HTTP Live Streaming（HLS）协议的视频文件格式

**单码率适配流**

```
#EXTM3U
#EXT-X-VERSION:3
#EXT-X-ALLOW-CACHE:YES
#EXT-X-MEDIA-SEQUENCE:2
#EXT-X-TARGETDURATION:16
#EXTINF:14.357, no desc
livestream-2.ts
#EXTINF:15.617, no desc
livestream-3.ts
#EXTINF:14.358, no desc
livestream-4.ts
#EXTINF:15.618, no desc
livestream-5.ts
#EXTINF:11.130, no desc
livestream-6.ts
```

该 m3u8 ⽂件只是⼀个简单的 Media Playlist。

**多码率适配流**

```
#EXTM3U
#EXT-X-STREAM-INF:PROGRAM-ID=1,BANDWIDTH=1280000
http://example.com/low.m3u8
#EXT-X-STREAM-INF:PROGRAM-ID=1,BANDWIDTH=2560000
http://example.com/mid.m3u8
#EXT-X-STREAM-INF:PROGRAM-ID=1,BANDWIDTH=7680000
http://example.com/hi.m3u8
#EXT-X-STREAM-INF:PROGRAM-ID=1,BANDWIDTH=65000,CODECS="mp4a.40.5"
http://example.com/audio-only.m3u8
```

包含多种⽐特率的 Master Playlist。该⽂件是⼀个实际使⽤中的**顶级 m3u8 ⽂件**，该⽂件中⼜定义了 http://example.com/low.m3u8 、 http://example.com/mid.m3u8 等 ⼏ 个 ⼆ 级 ⽂ 件 。 **顶 级 m3u8 ⽂件主要是做码率适配的，⼆级 m3u8 才是真正的切⽚⽂件**，客户端会**默认选择码率最⾼的请求**，如果发现码率达不到，会请求降低码率的流。**客户端拿到⼆级 m3u8 ⽂件后，会继续请求⾥⾯的⽂件**，这 时就可以进⾏播放了。



#### Tags

1. **#EXTM3U** ： 每个 m3u8 ⽂件第⼀⾏必须是这个 tag，如上⾯的两个示例。
2. **#EXT-X-VERSION**：m3u8⽂件版本号，⽐如`#EXT-X-VERSION:3`。
3. **#EXTINF** ：指定每个媒体段（ts）的持续时间，这个仅对其后⾯的 URI 有效，每两个媒体段 URI 间 被这个 tag 分隔开其格式为： `#EXTINF:,`
4. \#EXT-X-BYTERANGE ：表示媒体段是⼀个媒体 URI 资源中的⼀段，只对其后的 media URI 有效， 格式为： `#EXT-X-BYTERANGE:[@o]`
5. **#EXT-X-TARGETDURATION** ：指定当前视频流中的单个切⽚（即 ts）⽂件的最⼤时⻓（秒）。所以 #EXTINF 中指定的时间⻓度必须⼩于或是等于这个最⼤值。这个 tag 在**整个 Playlist ⽂件中只能出现 ⼀次**（在嵌套的情况下，⼀般有真正ts url 的 m3u8 才会出现该 tag）。格式为： `#EXT-XTARGETDURATION:`
6. **#EXT-X-MEDIA-SEQUENCE** ： 每⼀个 media URI 在 Playlist 中只有唯⼀的序号，相邻之间序号 +1。 格式为： `#EXT-X-MEDIA-SEQUENCE:` 。⼀个 media URI 并不是必须要包含的，如果没 有，默认为 0.
7. **#EXT-X-KEY** ：表示怎么对 media segments 进⾏解码。其作⽤范围是下次该 tag 出现前的所有 media URI。格式为： `#EXT-X-KEY:`
8. #EXT-X-PROGRAM-DATE-TIME ：将⼀个绝对时间或是⽇期和⼀个媒体段中的第⼀个 sample 相关 联，只对下⼀个 media URI 有效，格式如下： `#EXT-X-PROGRAM-DATE-TIME:`
9. #EXT-X-ALLOW-CACHE ： 是否允许做 cache，这个可以在 Playlist ⽂件中任意地⽅出现，并且最多只出现⼀次，作⽤效果是所 有的媒体段。格式如下： `#EXT-X-ALLOW-CACHE:`
10. #EXT-X-PLAYLIST-TYPE ：提供关于 Playlist 的可变性的信息，**这个对整个 Playlist ⽂件有效， 是可选的**，格式如下： #EXT-X-PLAYLIST-TYPE:
11. **#EXT-X-ENDLIST** ： 表示 m3u8 ⽂件的结束，**live m3u8 没有该 tag**。它可以在 Playlist 中任意位置出现，但是只能出现 ⼀个，格式如下： `#EXT-X-ENDLIST`
12. #EXT-X-MEDIA ：被⽤来在 Playlist 中表示相同内容的不同语种/译⽂的版本，⽐如可以通过使⽤ 3 个这种 tag 表示 3 种不同语⾳的⾳频，或者⽤ 2 个这个 tag 表示不同⻆度的 video。在 Playlist 中，这个标签是独⽴存在的，其格式如下： `#EXT-X-MEDIA:`
13. #EXT-X-STREAM-INF ：**指定⼀个包含多媒体信息的 media URI 作为 Playlist**，⼀般做 m3u8 的嵌 套使⽤，它只对紧跟后⾯的 URI 有效，格式如下： `#EXT-X-STREAM-INF:`
14. #EXT-X-DISCONTINUITY ：当遇到该 tag 的时候说明以下属性发⽣了变化：
    - file format ⽂件格式 
    - number and type of tracks 轨道 
    - encoding parameters 编码参数 
    - encoding sequence 编码序号 
    - timestamp sequence 时间戳序号
15.  #ZEN-TOTAL-DURATION ： 表示这个 m3u8 所含 ts 的总时间⻓度



### MP4

mp4⽂件格式⼜被称为MPEG-4 Part 14，出⾃MPEG-4标准第14部分 。它是⼀种多媒体格式容器，⼴泛 ⽤于包装视频和⾳频数据流、海报、字幕和元数据等。（顺便⼀提，⽬前流⾏的视频编码格式AVC/H264 定义在MPEG-4 Part 10）。 mp4⽂件格式基于Apple公司的QuickTime格式

mp4⽂件由box组成，每个box分为Header和Data。其中Header部分包含了box的类型和⼤⼩，Data包含 了⼦box或者数据，box可以嵌套⼦box。

#### Box种类

- **ftyp**：File Type Box，⼀般在⽂件的开始位置，描述的⽂件的版本、兼容协议等。
- **moov**：Movie Box，包含本⽂件中所有媒体数据的宏观描述信息以及每路媒体轨道的具体信息。⼀般位于放在⽂ 件末尾，但如果为了⽀持http边下载边播放则需要将moov提前。**注意，当改变moov位置时，内部⼀些值 需要重新计算。**
- **trak**：Track Box，记录媒体流信息，如视频、音频或字幕。⽂件中可以存在⼀个或多个track，它们之间是相互独⽴的。
- **mdia**：Media Box，这个盒子存储了关于媒体数据（视频、音频等）的信息，它包含有`mdhd`（Media Header Box）、`hdlr`（Handler Reference Box）和`minf`（Media Information Box）等子盒子。
- **mdat**：Media Data Box，存储实际的媒体数据，如音频和视频样本。这通常是MP4文件中最大的一个盒子，因为它包含了所有的媒体内容。
- **stbl**：Sample Table Box，这个盒子属于`minf`盒子的子盒子，包含了样本的详细信息，如样本大小和时间戳，这对于媒体的解码和播放至关重要。



### H264

H.264是在MPEG-4技术的基础之上建立起来的，其编解码流程主要包括5个部分：帧间和帧内预测、变换和反变换、量化和反量化、环路滤波、熵编码。H.264标准的主要目标是：与其它现有的视频编码标准相比，在相同的带宽下提供更加优秀的图象质量。

H264的x264默认使用**YUV420格式**进行编码

H.264 则针对视频数据经由⽹络传输的 需求，从功能上划分为两层，分别是视频编码层（VCL）以及⽹络抽象层（NAL）

- **VCL**：包括核⼼压缩引擎和块，宏块和⽚的语法级别定义，进行视频编解码，包括预测（帧内预测和帧间预测），DCT 变化和量化，熵编码和切分数据等功能，是为了实现更高的视频压缩比。**压缩切分后的 VCL 数据会包装成为 NAL 中的一部分。**
- **NAL**：负责将VCL产⽣的⽐特字符串适配到各种各样的⽹络和多元环境中，覆盖了所有⽚级 以上的语法级别

NALU是NAL的基本数据包，它包含一个原始数据块和一个NAL头部。⼀个原始的H.264 NALU单元**由[StartCode] [NALU Header] [NALU Payload]**三部分组成：

```
Start Code    NALU header    NALU payload    Start Code    NALU header    NALU payload
```

- Start Code ⽤于标示这是⼀个NALU 单元的开始，必须是"00 00 00 01" 或"00 00 01"
- NALU header占1个字节，储存⼀些该NALU的标志和类型信息，例如是否为关键帧 
- NALU header是数据负载，⻓度不定
- 对于FFmpeg解复⽤后，**MP4⽂件读取出来的packet是不带startcode，但TS⽂件读取出来 的packet带了startcode**



#### NALU

- **SPS**：序列参数集，包含影响整个视频序列的编码参数，这些参数对于解码一系列连续的图像（称为编码视频序列）至关重要，例如分辨率、帧率、帧结构（如帧是否交错）。
- **PPS**：图像参数集，包含一些影响如何解码单个图像（图像组或GOP）的参数，例如熵编码模式。它相较于SPS更频繁地更改和传输。
- I帧：帧内编码帧，可独⽴解码⽣成完整的图⽚。 
- P帧: 前向预测编码帧，需要参考其前⾯的⼀个I 或者B 来⽣成⼀张完整的图⽚。 
- B帧: 双向预测内插编码帧，则要参考其前⼀个I或者P帧及其后⾯的⼀个P帧来⽣成⼀张完整的 图⽚。 
- sps和pps都是非VCL的NAL数据类型，发的时候不需要带body
- 发I帧之前，⾄少要发⼀次SPS和PPS
- **如果码流或者编码参数发生改变**，一定要发一次新的SPS和PPS



#### 关键技术

##### 帧内预测编码

帧内编码用来缩减图像的空间冗余。为了提高H.264帧内编码的效率，在给定帧中充分利用相邻宏块的空间相关性，相邻的宏块通常含有相似的属性。可以根据周围的宏块预测（典型的是根据左上角的宏块，因为此宏块已经被编码处理），然后对预测值与实际值的差值进行编码，这样，相对于直接对该帧编码而言，可以大大减小码率。就是说一张图片分成很多个小块，一块区域内相同的小块只用存一个小块，其他的记录位置就行



##### 帧间预测编码

视频帧分为 I、P 和 B 帧三种帧类型

- **I-帧**：也成为关键帧，I-帧完全⾃我使⽤的，使用**帧内预测**，并且不使⽤任何其他帧的信息。它在三种帧中占最⼤的⽐例，并且具有最⾼的质量，但是压缩效率是最低的。 **⾃身可以通过视频解压算法解压成⼀张单独的完整的图⽚**
- **P -帧**：P -帧是所谓的“预示”帧。当创建了⼀个P-帧时，编码器可以向后查看I-帧或者P-帧中冗余的 图⽚信息。P-帧⽐I-帧效率⾼，但是没有B-帧的效率⾼。 **需要参考其前⾯的⼀个I frame 或者P frame来⽣成⼀张完整 的图⽚**
- **B-帧**：B-帧是双向预测帧，在压缩时，它即参考前而的帧，又参考它后面的帧。采用帧间压缩技术。B 帧记录的是本帧与前后帧的差别，换言之，要解码 B 帧，不仅要取得之前的缓存画面，还要解码之后的画面，通过前后画面的与本帧数据的叠加取得最终的画面。B 帧压缩率高，但是解码时 CPU 工作量会比较。这使得B-帧在三种帧中具备最佳的效率。注意，B-帧在使⽤Baseline⽅式⽣产视频的时候是不可⽤ 的。**要参考其前⼀个I或者P帧及其后⾯的⼀个P帧来⽣成⼀张完 整的图⽚，无法参考B帧，且会带来第二延迟，因为需要参考后面的帧**
- **IDR帧**。**I帧**的一种，一个GOP序列的第一帧，立刻刷新，使错误不致传播，从 IDR 帧开始，重新算一个新的序列开  始编码。一个GOP一半是1秒帧数的1倍或2倍
- 

帧间预测中，按照分块标准将⼀帧图像划分成不同⼤⼩的分块，通过参考帧中⼤⼩相同的宏块进⾏预测得 出编码的宏块，其中计算产⽣运动⽮量即两个宏块之间的偏移距离。然后使⽤参考宏块和预测块计算出残 差数据，选⽤更⼩的像素为基准进⾏运动估计，求得的运动⽮量更加准确，运动⽮量精度的提⾼能够减少 残差，如此能够使得在提⾼了压缩率的同时，更好的保证了视频质量。



##### 整数变换

在变换方面，H.264使用了基于4×4像素块的类似于DCT（整数变换）的变换，但使用的是以整数为基础的空间变换，不存在反变换，通过DCT等变换，可以有效地从图像数据中提取出能量集中的频率分量，而忽略人眼不易察觉的高频部分，从而减少数据量，因为取舍而存在误差的问题。此外，整数DCT变换还具有减少运算量和复杂度，有利于向定点DSP移植的优点。



##### 量化

- H.264中可选32种不同的量化步长，这与H.263中有31个量化步长很相似，但是在H.264中，步长是以12.5%的复合率递进的，而不是一个固定常数。
- **我们让变换块的系数都同时除以一个值，这个值我们称之为量化步长**，也就是 QStep（QStep 是 编码器内部的概念，用户一般使用量化参数 QP 这个值，QP 和 QStep 得到的结果就是量化后的 系数。QStep 越大，得到量化后的系数就会越小。同时，**相同的 QStep 值，高频系数值相比低频 系数值更小，量化后就更容易变成 0。这样一来，将大部分高频系数变成 0**。
- **解码的时候，需要将 QStep 乘以量化后的系数得到变换系数**，很明显这个变换系数和原始没有量化的变换系数是不一样的，这个就是常说的**有损编码**。 
- 而到底损失多少呢？其由 QStep 来控制，**QStep 越大，损失就越大**。QStep 跟 QP 一一对应。从 编码器应用角度来看，QP 值越大，损失就越大，从而画面的清晰度就会越低。同时，QP 值越大 系数被量化成 0 的概率就越大，这样编码之后码流大小就会越小，压缩就会越高。



##### 熵编码

视频编码处理的最后一步就是熵编码，在H.264中采用了两种不同的熵编码方法：通用可变长编码（UVLC）和基于文本的自适应二进制算术编码（CABAC）。



### AAC

AAC是⼀种由MPEG-4 标准定义的有损⾳频压缩格式

- ADIF：⾳频数据交换格式。这种格式的特 征是可以确定的找到这个⾳频数据的开始，不需进⾏在⾳频数据流中间开始 的解码，即它的解码必须在明确定义的开始处进⾏。故这种格式常⽤在磁盘 ⽂件中。
- ADTS：AAC⾳频的传输流格 式。AAC⾳频格式在MPEG-2（ISO-13318-7 2003）中有定义。AAC后来 ⼜被采⽤到MPEG-4标准中。这种格式的特征是它是⼀个有同步字的⽐特 流，解码可以在这个流中任何位置开始。它的特征类似于mp3数据流格式。

总之，ADTS可以在任意帧解码，也就是说它每⼀帧都有头信息。ADIF只有⼀ 个统⼀的头，所以必须得到所有的数据后解码。

如果编码AAC裸流的时候，遇到写出来的AAC⽂件并不能在PC和⼿机上播放，很⼤的可能就是AAC⽂件的每⼀帧⾥缺少了ADTS头信息⽂件的包装拼接。 只需要加⼊头⽂件ADTS即可。

AAC⾳频⽂件的每⼀帧由ADTS Header和AAC Audio Data组成，每⼀帧的ADTS的头⽂件都包含了⾳频的采样率，声道，帧⻓度等信息，ADTS的头分为固定头信息，紧接着是可变头信息，存储⼀个ADTS帧的⻓度、码流等信息



### YUV

- “Y”表示明亮度（Luminance或Luma），也就是灰阶值，“U”和“V”表 示的则是色度（Chrominance或Chroma）。
- YUV用于压缩RGB后进行传输。在传输过程中，为了有效减少数据量，YUV格式常被用于视频压缩。比如，Y通道保持全分辨率，而U和V色度通道可以在不太影响视觉质量的情况下进行降采样。
- 与我们熟知RGB类似，YUV也是一种颜色编码方法，它是指将**亮度参量（Y：Luminance 或Luma）和色度参量（UV：Chrominance或Chroma）分开进行表示**的像素编码格式。
- 这样分开的好处就是不但可以避免相互干扰，没有UV信息一样可以显示完整的图像， 因而**解决了彩色电视与黑白电视的兼容**问题；还可以**降低色度的采样率**而不会对图像质量影响太大，降低了视屏信号传输时对频宽（带宽）的要求。
- Y Y共用一组UV分量
- YUV是一个比较笼统地说法，针对它的具体排列方式，可以分为很多种具体的格式：
  - **打包（packed）格式**：将每个像素点的Y、U、V分量**交叉排列并以像素点为单元连续的存放**在同一数组中，通常几个相邻的像素组成一个宏像素（macro-pixel）
  - **平面（planar）格式**：使用**三个数组分开连续**的存放Y、U、V三个分量，即Y、U、V分别存 放在各自的数组中。



#### YUV采样表示法

- YUV采用A:B:C表示法来描述**Y,U,V采样频率比例**，下图中黑点表示采样像素点Y分量， **空心圆表示采样像素点的UV分量**。主要分为 YUV 4:4:4、YUV 4:2:2、YUV 4:2:0 这几种 常用的类型
- 4:4:4 表示色度频道没有下采样，**即一个Y分量对应着一个U分量和一个V分量**。
- 4:2:2 表示 2:1 的水平下采样，没有垂直下采样，即每**两个Y分量共用一个U分量和一个V分量**。
- 4:1:1 表示水平4个像素中Y取样4个，U取样1个，V取样1个
- 4:2:0 表示 2:1 的水平下采样，2:1 的垂直下采样，即每**四个Y分量共用一个U分量和一个V分量**。



#### YUV数据存储

下面以每个分量数据存储在一个char（或byte）中为例描述YUV的数据存储方式。 

1. **4:4:4格式** ，对应Ffmpeg像素表示AV_PIX_FMT_YUV444P
2. **4:2:2格式**
   - **YUV422P**：对应Ffmpeg像素表示AV_PIX_FMT_YUV422P，该类型为**planar格式**
   - **YUV420P**：对应Ffmpeg像素表示AV_PIX_FMT_YUV420P，该类型为**planar格式**，(4+1+1)/4 = 1.5字节
3. **4:2:0格式**：NV12，对应Ffmpeg像素表示AV_PIX_FMT_NV12



#### YUV Stride对齐问题

比如分辨率638x480的YUV420P图像，我们在内存处理的时候如果要以16字 节对齐，则638不能被16整除，我们需要在每行尾部填充2个字节。就是640。 此时该图片的Y stride为640字节。



### RGB

R、G、B 分别表示红绿蓝三个通道的值。RGB 格式根据存储的位数可以分为 16 位格式 、 24 位格式 和 32 位格式。RGB主要用于显示器的显示。

16 位格式主要是 RGB555 和 RGB565，这种格式为红色、绿色和蓝色每个颜色通道分配5位，共15位每像素。这样每个通道有32个不同的强度级别，从0到31，整体可以表示大约32768种不同的颜色。RGB555格式通常还有一个额外的空白位，使得总位数达到16位，这是为了内存对齐方便。

24位主要是RGB888，这种格式为每个颜色通道（红色、绿色、蓝色）分配8位，共24位每像素。这意味着每个颜色通道可以有256个不同的强度级别，从0到255，使得可以表示大约1677万种颜色。

32位主要是ARGB8888，这种格式不仅包含RGB（红、绿、蓝）三个颜色通道，每个通道8位，总共24位，而且还额外增加了一个8位的透明度通道（Alpha channel），使得每个像素总共有32位。



# 项目

## 基于QT的股票量化交易软件平台

这是一个基于qt，运行在window平台的股票量化交易平台，虽然市面上有现有的量化交易平台，例如mt4，但是这些平台都比较老，所以项目立项之初的目的是做一个界面更加美观，功能更加强大的软件。这个项目分为很多模块，有网络模块/数据中心模块/图表绘制模块/交易模块/快讯模块 /消息中心模块，我在这个项目里的主要工作是

### 使用asio库重构网络层

原生的SOCKET用起来不方便，封装性不好，如果要实现高级的功能用起来比较麻烦，且不支持跨平台；因此使用boost库重构网络层。

流程：

1. 创建io_context
2. 创建resolver，填入ip与端口
3. 调用async_connect函数连接
4. 连接成功后调用自定义函数do_read，里面调用async_read_some函数指定缓冲区与处理函数
5. 收到数据后调用之前绑定的自定义read_some函数，在里面处理，并且调用自定义函数do_read
6. 期间如果收到了数据会执行自定义read_some函数，并不会造成栈溢出



### 数据中心消息格式

一个模块要发送数据给后端，会先把数据填到自定义的结构体，然后把这个结构体传到数据中心，数据中心会先将这个结构体序列化，然后把数据传给网络层，网络层会把这个数据填入网络层数据包的data变量里，最后会按照包头、包长度、包信息类型、包数据、校验和拼接数据包，发送给服务器。校验和是所有数据加起来的长度。



### 设计链式缓冲队列

链式缓冲队列的用处是消息同步，防止在多线程的条件下出现竞态条件，在本项目的使用地方是，网络层得到一个完整的数据包后，会将该数据包丢到**数据中心的接收链式缓冲队列**里，**解析多线程**会从**接收链式队列里提取数据包**进行解析，解析完后会抛到数据中心的**发布链式缓冲队列**里，数据中心的发布线程会从**发布链式缓冲队列里一个数据包一个数据包**那样获取，然后**使用观察者模式**将数据发送到各个界面模块里。

该链式缓冲队列是一个RAII锁环形队列，采用unique_lock来实现。不使用原子操作是因为每个线程解析数据包需要一定的时间，且不是服务器，并发量做不到太高，没必要用原子操作。

**锁**

**互斥锁（mutex）**

可以避免多个线程在某一时刻同时操作一个共享资源

eg:

```C++
std::mutex mtx;
mtx.try_lock();
mtx.lock();
```



**lock_guard**

建议使用**lock_guard**替代直接使用mutext.lock()、mutext.unlock()，lock_guard会在构造方法里mutext.lock()，析构方法里mutext.unlock()，主要为了避免中途异常退出导致mutext没有unlock。



**unique_lock**

**lock_guard**只能在析构的时候解锁，不提供unlock接口，不够方便，unique_lock提供lock和unlock接口，更灵活，析构函数会判断是否释放锁，没有释放会调用unlock函数



**条件锁（condition_variable）**

条件锁就是所谓的条件变量，某一个线程因为某个条件为满足时可以使用条件变量使改程序处于阻塞状态。一旦条件满足以“信号量”的方式唤醒一个因为该条件而被阻塞的线程

eg:

```C++
std::mutex mutex;
std::condition_variable cond;
std::deque<int> q;
cond.wait(locker, [](){return !q.empty();});// q不为空则得到锁，唤醒线程
```



**递归锁（recursive_mutex）**

在一些业务场景下，线程在获取锁后还会多次获取该锁，且保证不出现死锁或者**程序崩溃异常**。简单地说，递归锁是一种在**锁已被持有的情况下，允许同一线程对该锁进行多次加锁的机制**，也叫**可重入锁**。

递归锁中会**记录加锁的次数**，每次加锁，计数+1，且判断**如果已经锁住**，不做处理，每次解锁，计数-1，所以使用时需要我们做好对等解锁操作。



**自旋锁（std::atomic_flag）**

- 自旋锁是一种基础的**同步原语**，用于保障对共享数据的互斥访问。
- 与互斥锁的相比，在获取锁失败的时候**不会使得线程阻塞而是一直自旋尝试获取锁**。
- 当线程等待自旋锁的时候，CPU不能做其他事情，而是一直处于**轮询忙等**的状态。
- 自旋锁主要适用于**被持有时间短**，线程不希望在**重新调度上花过多时间**的情况。
- 实际上许多其他类型的锁在底层使用了自旋锁实现，例如多数互斥锁**在试图获取锁的时候会先自旋一小段时间**，然后才会休眠。
- 如果在持锁时间很长的场景下使用自旋锁，则会导致CPU在这个线程的时间片用尽之前一直消耗在无意义的忙等上，造成计算资源的浪费。

eg:

```C++
std::atomic_flag lock = ATOMIC_FLAG_INIT;
while (lock.test_and_set(std::memory_order_acquire))  // 获得锁
      ; // 自旋
```



**读写锁（std::shared_mutex）**

所谓「读写锁」，就是同时可以被多个读者拥有，但是只能被一个写者拥有的锁。而所谓「多个读者、单个写者」，并非指程序中只有一个写者（线程），而是说不能有多个写者同时去写。

eg:

```C++
 std::shared_lock<std::shared_mutex> lock(mutex_);// 读，能被多个线程共享
 std::unique_lock<std::shared_mutex> lock(mutex_);// 写，只能有1个线程进行写
```



### 将原有的KV解析协议改为protobuf协议

两个原因，第一个原有的KV协议在遇到传输的内容里刚好规定的字符的话，就会出现解析错误；第二个这个软件属于交易软件，服务器需要不断给客户端发送数据，因此需要解析的数据比较多，服务器发送需要占用的数据很多，所以需要考虑数据压缩。因此protobuf协议刚好能解决这两个问题

**protobuf**

- Protobuf是Protocol Buffers的简称，它是Google公司开发的一种数据描述语言，是一种轻便高效的结构化数据存储格式，可以用于结构化数据串行化，或者说序列化 。
- 序列化后体积相比Json和XML很小，适合网络传输
- 序列化反序列化速度很快，快于Json的处理速度
- 消息格式升级和兼容性还不错
- 使用的时候需要编写proto文件，然后使用proto库进行编译得到pd.h文件和pd.cc文件，然后项目里引用这个文件即可

**语法**

- required 表示该属性为必选属性，否则对应的 message“未初始化”，debug 模式下导致 断言， release模式下解析失败；
- optional 表示该属性为可选属性，不指定，使用默认值（ int 或者 char 数据类型默认为 0,string 默认为空， bool 默认为 false，嵌套 message默认为构造，枚举则为第一个）
- repeated 表示该属性为重复字段，可看作是动态数组，类似于 C++中的 vector。
- 如果为 optional 属性，发送端没有包含该属性，则接收端在解析式采用默认值。对于默认值，如果已设置默认值，则采用默认值，如果未设置，则类型特定的默认值为使用，例如 string 的默认值为 ” ”。



### 使用完整的观察者模式重构数据中心的观察者模式

原来的观察者模式是**仅有一个发布者，并且订阅者都是私有订阅**，这样不符合观察者模式，且后续还有需求是**一个消息要两个以上订阅者同时收到**，因此需要修改。修改方式为使用**一个发布者map存储所有发布者**，发布者对象里有一个**订阅者数组**，每条请求的消息都有一个**存储需要私有的信息**，用于有私有发送的情况。

除此自外，订阅者类接收到消息后，会使用存储**自定义信号**与**带有该自定义消息处理的函数的类的QObject指针**，将消息抛到消息队列，让消息处理的类来处理这个消息；这样做的目的是**方便分层**，数据中心管理者增加发布者与订阅者即可，数据通过事件循环传给其他模块做自定义处理

**观察者模式**

观察者模式属于**行为型模式**。在程序设计中，观察者模式通常由两个对象组成：观察者和被观察者。当被观察者状态发生改变时，它会通知所有的观察者对象，使他们能够及时做出响应，所以也被称作“发布-订阅模式”。



### 使用mvc模式对网络层/UI、控制层进行重构

原本的主界面窗口负责了太多职责，比如数据同步的全流程等本应该是界面控制层的职责，这样不利于后面的维护与功能扩展，所以就要进行重构。具体重构就是重新按照职责对主窗口界面的函数进行划分，应该是界面控制层的功能函数就移植到界面控制层，并且做好函数里功能职责的分块。比如同步的操作不要放在主界面执行，而是放到控制层执行，主界面仅仅负责通知控制层开启同步的操作。

**MVC**

- V表示view，即显示层、表示层、界⾯层。界⾯层不关⼼具体逻辑，也不关⼼具体数据。如果界⾯层需要数据，则开放接⼝或者属性，让其它层来调⽤。注意，界⾯层需要处理⽤户的交互。将⽤户的⾏为反馈给控制层，但是任何处理需要依 据具体的情况来定。如果是界⾯层的处理，则⾃⼰完成；如果还涉及业务处理，则交由控制层来处理。
- M表示model，即模型层。模型层只关⼼数据模型，对于界⾯层任何展示、控制层任何使⽤，毫不关⼼。如果外部需要使⽤某些数据，或者进⾏某些数据处理，模型层会开放接⼝出来，供外部使⽤。 
- C表示controller，即控制层。控制层是MVC的核⼼，这层的代码可复⽤性最差，⼀般都是和具体业务深度绑定的。控制层负责整个产品的逻辑和调度。从界⾯层来的⽤户请求，到模型层的数据摘选，以及业务进度的控制，都是在这⼀层完成的。



### 使用MVD模式重构消息中心模块

原有的消息中心模块并没有采用MVD模式设计，所以实现起来比较复杂，原有的架构是一个管理类，一个主窗口类，一个信息展示类，一个信息类，一个详细消息展示类，且所有消息的管理都是通过信息展示类。采用MVD模式重构后，界面变成管理类控制外界交互，且负责消息存储，消息展示类使用MVD模式来展示所有消息，详细消息展示类负责详细展示。这样重构后类的数量少了很多，且信息展示类里的各个控件不用自己实现，例如滚轮。

**MVD模式**

MVD模式通常指的是**Model-View-Delegate**架构，这是一种用于分离数据的逻辑表示（Model）、数据的视觉表示（View）以及数据呈现细节（Delegate）的设计模式。这种模式使得数据表示、数据操作和数据展示之间的耦合度降低，提高了应用程序的灵活性和可维护性。

**Model（模型）**

- **作用**：模型是用来管理数据和业务逻辑的部分，它定义了数据如何被存取，同时也负责数据的有效性验证和数据状态的更改通知。
- **类型**：Qt提供了多种标准模型类，例如`QStandardItemModel`、`QStringListModel`、`QFileSystemModel`等，也可以通过继承`QAbstractItemModel`来创建自定义模型。

**View（视图）**

- **作用**：视图用来呈现模型数据，它从模型中获取数据并生成用户界面。视图不处理数据本身，只负责展示数据和用户交互。
- **类型**：Qt中常见的视图类包括`QListView`、`QTableView`和`QTreeView`等，这些视图可以直接与模型连接显示数据。

**Delegate（代理）**

- **作用**：代理控制视图中的单元格如何显示数据（例如文字和图标），以及如何编辑这些数据。代理可以为不同的数据类型提供定制的渲染和编辑方式。
- **定制**：通过继承`QStyledItemDelegate`或`QItemDelegate`，可以实现自定义的单元格表现和编辑行为。



### 使用多线程实现回测功能

回测的作用是将**用户编写的策略放到以往的行情数据中进行模拟交易**，测试交易效果。在我们项目的回测里，我们是**一个策略就1个线程**，可以**多个策略同时回测**，线程池用的是**QT的全局线程池**。回测具体流程是先根据参数从服务器下载指定的历史数据，然后启动策略线程，不断喂投tick，根据这个tick计算订单数据，如果这个数据达到用户设置的处理条件，就调用用户自己编写的逻辑，然后将数据展示到界面上，从此往复。最后回测完成后，将所有数据综合计算，得出回测结果，如收益率。



### 遇到什么问题，如何解决

在重构数据中心的观察者模式的时候，我发现重构完后在程序运行一段时间后，有些功能会被二次甚至多次执行，这种现象看起来像是一个数据被多次通知执行，然后我就在数据中心发布者发布消息的地方打断点观察，发现观察者数组的大小一直没有变化，有一些本该析构掉的对象没有析构掉，然后我发现我的观察者数组的类型是shared_ptr，shared_ptr会导致资源循环引用无法正常析构，因此将shared_ptr改给weak_ptr即可解决这个问题。



### 其他技术

#### UML类图

**依赖关系**（用虚箭线表示）

就是构造这个类的时候，需要依赖其他的类，通常表现为某个类的方法使用了另一个类，比如：动物依赖水和氧气



**继承、泛化关系**（用带空心三角形的实线表示）

指定了子类如何特化父类的所有特征和行为。例如：鸟是动物的一种



**实现关系**（用带空心三角形的虚线表示）

表示类是接口所有特征和行为的实现



**关联关系**（用实箭线表示）

这个类有一个属性是其他类，通常涉及在类中使用另一个类的实例，联可以是双向的或单向的，比如：教授属于一个部门，而部门拥有多位教授。



**聚合关系**（用带空心菱形的实线表示）

聚合关系是关联关系的一种，是强的关联关系 ；

特点： 部分对象的生命周期并不由整体对象来管理。也就是说，当整体对象已经不存在的时候，部分的对象还是可能继续存在的。比如：一只大雁脱离了雁群，依然是可以继续存活的。



**组合关系**（用带实心菱形的实线表示）

组合关系同样是关联关系的一种，是比聚合关系还要强的关系。

特点：在组合中，部分与整体生命期一致，部分与组合同时创建并同时消亡 。比如：鸟与翅膀的关系。



#### SQLITE

**创建表**

create table 表名(字段名称 数据类型 约束，字段名称 数据类型 约束，。。。。。)

**删除表**

drop person

**插入数据**

insert into 表名【字段，字段】 values（值1，值2，。。。。）

**修改数据**

update 表名 set 字段=新值 where 修改的条件

**删除数据**

delete from 表名 where 删除的条件

**查询语句**

select 字段名 from 表名 where 查询条件 group by 分组的字段 having 筛选条件 order by 排序字段



## 基于ffmpeg的网课点播系统

这是一个基于window的网课点播系统，用于解决高校网课数据外泄和学生在线观看的问题。项目有客户端以及流媒体服务器。客户端采用qml+ffmpeg的方式进行实现，具有登录、点播、倍速、停止、下一个媒体等常见播放器的业务逻辑，还有普通播放功能以及OTA升级功能。流媒体服务器采用epoll+线程池来实现高并发，使用mysql进行数据持久化存储，redis进行缓存存储，采用HLS协议进行m3u8推送。



### 整个点播流程

在用户点击播放列表里的播放项后，客户端会使用http协议向服务器请求该播放项的m3u8文件，服务器会使用http协议将m3u8文件发送给客户端。这个m3u8文件里的ts路径只有文件名，所以客户端不断请求m3u8里记录的ts文件时，就会将之前登录的时候，服务器发送来的url前缀添加到请求路径里，向服务器请求这个ts文件。

客户端的读取线程会把这些ts文件依次打开，将里面的包读取到对应的包队列里，客户端会有一个所有包队列长度上限，上限是30s，达到长度后会停止请求新的ts文件。客户端的视频解析和音频解析线程会不断对应队列里提取包出来解析。对于音频，在音频帧队列里有帧的时候，会进行重采样，然后更新音频时钟，最后播放。对于视频，会进行视频显示delay计算，然后根据delay决定如何播放该视频帧。



### 音视频同步是如何实现的

有两个时钟，分别为音频时钟以及视频时钟，其中音频时钟作为主时钟。

每次读取完音频的一帧后就会让音频时钟增加`重采样大小 / 采样率的值`

每次展示一视频帧的时候视频时钟会让视频时钟增加帧率分之一，且加上解码时得到的额外显示次数的二分之一，视频时钟的作用是用来矫正每一帧的pts，将上一帧的结束时间赋值到当前这一帧转成RGB格式的AVFrame的pts上

对于同步：

1. 先拿当前播放帧的pts减去上一帧的pts得到delay
2. 音频时钟减去未播放的音频缓冲区，让要播放的那一帧的pts减去音频时钟得到diff
3. 这时候进行delay与diff的比较
4. 如果delay大于等于负diff，表示视频落后音频，delay置0后续立即播放
5. 如果delay小于等于负diff，表示视频快于音频，就乘以2

也就是说

```
-delay               delay  
  |        diff        |
  ----------------------
慢，立即播放         快，延迟*2
```



### 倍速播放如何实现

我是通过修改音频的采样率，然后播放音频的时候会加快音频时钟的增加，从而加快视频帧的播放



### 如何播放视频与音频

视频使用Qlabel绘制，音频使用SDL播放，其中音频的播放用到了SDL的回调函数，在回调函数里读取帧队列然后放到缓冲区里就能让SDL播放



### 跳转播放如何实现

发生跳转后，首先会计算跳转的位置，会将跳转标志位置1，然后读取线程会将当前序列号+1，这样包队列和帧队列会丢弃所有包和帧，并且会刷新解码器。如果跳转的位置大于当前缓存的最新位置，则丢弃所有缓存，从跳转位置的那个TS文件重新开始下载；如果跳转的位置小于当前缓存的最新位置，则从这个位置的TS文件开始播放。打开第一个TS文件后，正常读取解码，但是最后一步展示的时候，因为采用hls协议建议ts文件时长，所以一个ts文件是10s，所以能计算跳转位置在这10s的长度的位置，然后跳过跳转位置之前的所有帧。

对于视频时钟以及音频时钟，发生跳转后，音频时钟会增加对应的秒数



### 直播延迟讲一讲

延迟的意思是视频被捕获和在观众屏幕上显示之间的时间延迟，直播延迟一般是由于缓存引起的。缓存主要是用来缓解网络抖动、服务器转发更有效率、网络阻塞后拉流端取得阻塞这段时间缺失的数据的问题。

**推流端**

- 音频帧1024个采样点作为一帧数据编码，采样率48khz，1秒采集48000次数据，1音频帧采集需要21.3ms

- 编码的时候B帧需要前后参考，且编码出来的帧是按照dts顺序的，所以该B帧需要等待后续的P帧或I帧的出现才能把这一帧数据发送出去，所以直播不用B帧

- 有缓存队列，如果网络不好，音频帧和视频帧就先放到缓存队列里，等到网络回复就再发送




**SRS服务器**

- tcp_nodelay用于控制**Nagle算法**，一般关掉，防止粘包

- 服务器可能会进行gop缓冲，用于支持画面秒开， 不然会发生发送出去的第一帧不是I帧的情况，无法秒开，一般GOP是1秒-2秒

- 有多个拉流端，每个拉流都有一个缓存队列

- 每次发送数据不是一帧来就发送的，会休眠一段时间再从缓存队列里取数据发送，降低cpu占用率




**拉流端**

- 拉流端出现网络波动，服务器缓存了3秒速度，拉流端回复正常网络，然后拉取数据得到3秒数据，这时候如果一直播放，就一直后3秒缓存，所以需要倍数播放，更快消耗缓存

- 需要设置一个阈值，比如可以缓存超过300ms就倍数播放，小于200ms就正常播放，不然很容易触发倍数播放

- 如果不支持变速播放，要低延迟只能drop数据

- 一般是1.5倍速



### 使用什么流传输

- **ES**：只包含一种`内容的数据流`，如只含`视频`或只含`音频`等。最基本的音视频流，通常作为TS流和PS流的基础。
- **PS**：PS 包的包头中包含了`同步信息`与`时钟恢复信息`。一个 PS 包最多可包含具有`同一时钟基准`的 16 个视频 PES 包和 32 个音频 PES 包。由可变长度的组成，适合可靠的传输环境。容错能力较低，因为主要用于稳定的存储介质。主要用于存储和播放场景，如DVD-Video。
- **TS**：广泛用于数字电视广播（如DVB、ATSC）和流媒体传输。由固定长度的包组成，每个包通常为188字节。设计为支持不可靠的传输环境，具有较的错误恢复能力。广泛用于数字电视广播（如DVB、ATSC）和流媒体传输。

我这里使用的是ES流



### 为什么要用redis

redis主要起一个缓存用户信息的作用。服务器为了防止有人恶意攻击，数据库恶意写入，因此没有做注册的业务，注册业务由人工通过专门的工具完成。在专门工具写入mysql的时候，mysql会通过udf来将用户信息写入到redis。



### 介绍一下UDF

UDF(user-defined function)是MySQL的一个拓展接口，也可称之为用户自定义函数，在本项目里，充当mysql做了增删改后触发器调用UDF更新数据到redis的角色。UDF一定要包含主函数、初始化函数、添加函数、析构函数，本项目就是在添加函数里，通过传入的数据库操作对redis进行修改。主函数返回类型有3种，分别为string、int、double，本项目采用string，返回用户id的md5值，这个md5值在前面添加函数就已经得到。

其他的话还有clear函数和reset函数，clear函数在分组后就调用清理数据，reset函数在group by第一次执行



**线程池与IOCP在项目的应用**
答：首先创建IOCP，然后把服务器套接字与IO端口绑定，使用acceptex函数投递到内核。其次把连接进来的客户端套接字与IO端口绑定，使用WSARecv等异步函数投递到内核。IOCP收到客户端发来的数据后，内核会进行处理，将数据从网卡缓冲区发送到用户缓冲区，写完后就会把事件设置为激活，这时线程池里的函数就会获取重叠结构投递过来的数据，然后对这个数据做对应任务处理。



**线程池如何添加新请求**
答：每个线程类都有一个**CFUNCTION类**，这个类用于**存储任务函数**，这个对象是**一个模板类**，使用模板类有两个原因，一个是**模板类操控可变参数更容易**，通过完美转发来存储不同数量的参数与目标函数，另一个是**线程类不能是模板类**，但是每次**需要执行的函数是不同的**，因此就需要一个存储任务函数的模板类。另外，添加新请求还要考虑**线程安全**，所以线程池的任务函数分发就需要用到IOCP，因为**IOCP可以在线程安全的条件下进行消息循环**。由于IOCP的消息投递**需要用到Overlapped结构体**，因此还需要一个**Task类保存任务函数**，将任务函数投递到空闲线程中，让空闲线程执行。在这个CFUNCTION类与IOCP的线程安全的前提下，线程池就能通过投递存储了任务函数的task类对象来添加新的请求。



**说说IO常见模型**
答：常见的IO模型分为4种，分别为同步阻塞、同步非阻塞、IO多路复用与异步IO。同步阻塞就是要等到IO操作完成才返回，不然一直阻塞住，这种方式很浪费资源。同步非阻塞就是不等待IO操作完成才返回，而是投递就返回，这种方式会一直轮询内核占用大量的CPU时间、效率低下。
		IO多路复用就是同步 IO 模型，一个线程可以监视多个文件句柄，轮询检测IO的状态，如果有描述符就绪，就将该文件符加入到队列里，等待用户提取。用户提取就绪文件符，然后逐个处理IO事件，处理过程是阻塞状态，处理完本次提取的才处理新的。这种方式可以让一个线程管理多个IO操作。
		异步IO复用是指异步IO模型，跟IO多路复用一样一个线程监视多个套接字，向内核注册某个IO操作后，内核配合网卡完成整个IO操作后通知用户，让用户执行后续操作。



**说说IOCP怎么使用**
答：创建IO完全端口后，将服务器的套接字与完全端口绑定。有客户端连接上来后，将完全端口与当前客户端的套接字绑定，监听客户端的套接字。



### 登录逻辑是怎样的

客户端打开后先与服务器进行RSA公钥互换，服务器使用客户端的公钥发送AES，后续都是通过AES加密。用户输入信息点击登录后，客户端会对用户的账户密码进行MD5签名，然后对这些数据进行AES加密发送到服务器。服务器查询Redis后确认信息正确，就把状态与播放列表发给用户端。



**互斥锁或IOCP下收发信息有什么效率差距**
答：我做的测试是使用IOCP实现一个线程安全队列，测试1秒能往里面添加多少次数据。11代i7下互斥锁1秒10万次左右，IOCP1秒20万次左右。



**IOCP缺点**
答：无法在Linux下用；
	**开发与维护成本更高**，因为IOCP需要用到**异步函数**，难以理解；
	用ex好处是**提前准备套接字与缓存**，连接成功就代表已经存储好了，直接用就可以了，提高效率。如果因为并发太高，此时端口用完，提前准备套接字就会失败，就不会等待连接，不用ex的话，物理层面连接成功，软件层面连接失败，客户端又在挂起状态，就会出问题



### 普通播放逻辑

学生在观看直播的时候可以录制，回放的时候一路依次读取 AVPackage 后送到 H264 解码器解码，解码 拿到 YUV 数据后进行格式转换 RGB888,通过信号和槽机制将 QImage送到窗口上的 QLabel 刷新显示预览,期间会叠加部分水印；



### 水印如何叠加

在QImage展示到QLable前，我会将使用Qpainter对QImage进行水印绘制，水印内容是用户的账号



### 遇到了什么问题，怎么解决

在开发跳转的时候，发现跳转后会出现花屏，花屏持续几秒后，视频逐渐恢复正常。因此我就思考为什么会这样，h264帧间预测使得各帧之间有依赖，这个花屏有可能是跳转的那一帧解码使用了跳转前的那些帧，后续回复正常是因为读取到了IDR帧，。因此我在跳转后清空包队列的时候，加上一个刷新包，通知刷新解码器，然后跳转播放就没有问题了。



## 长沙市岳麓区老年监护系统（CMS系统）

本项目是一个客户端基于ffmpeg的远程监护系统，设计的目的是出于实际业务和保密需求，需定制音视频流媒体中控台软件。本项目拥有客户端、服务器与摄像头嵌入式程序，客户端采用自定义协议作为拉流播放，服务器采用epoll作为多路复用，嵌入式程序负责连接服务器，使用自定义datasheet与客户端进行交互，客户端主要是作为一个中控的作用，同时监控多个摄像头，服务器主要是作为一个用户登录，数据存储的功能。



### P2P是如何实现的

摄像头连接电源与连接网络后，首先会与tutk打洞，打完以后会自动与本项目的公网服务器进行连接，然后公网服务器会保存这些摄像头的IP与端口。如果客户端登录上来之后，公网服务器会将这些摄像头的IP与端口发送给客户端，客户端就使用TUTK打洞连接摄像头。

一个session2块钱



### 重构讲讲

第一版的代码，公网服务器做摄像头的 IP 与端口存储，并没有 P2P 连接，所有摄像头的视频音频都需要经过公网服务器这一个中转，摄像头开机后会主动连接到服务器，公网服务器对摄像头进行 RTSP 连接，然后数据经过自定义协议发送到客户端进行展示。

重构的原因是服务器做中转的话对带宽消耗过大，客户端观看质量很差，所以考虑使用P2P完成视频传输。

第二版的代码，摄像头开机后会连接TUTK进行打洞，打完后会与公网服务器进行连接，让公网服务器记录摄像头的UUID、IP以及端口，摄像头会一直向TUTK发送心跳包维持住这个洞。客户端登陆后公网服务器会发送所有摄像头的UUID、IP以及端口，客户端就使用TUTK打洞与摄像头进行UDP连接即可。

我在重构方面具体的工作是对于客户端，增加摄像头的P2P连接，数据交互用的原来的datasheet。对于服务器，我是屏蔽掉连接RTSP交互的代码与中转视频发送的代码。



### 云台控制功能有那些

例如摄像头的上下左右转动



### TUTK打洞的流程

首先摄像头刚刚开机会主动连接tutk服务器，然后客户端也会连接tutk服务器，然后tutk会通知摄像头向客户端发送一个数据包，然后客户端就向摄像头发送一个数据包，这样就通了。如果说在局域网内，就需要路由器支持回环转化。



### 客户端是怎么设计的

客户端的整体架构是mvc设计模式，分为v层界面层、m层数据包层、c层控制层。在



### 托盘怎么实现

用QT的QSystemTrayIcon



### 推送的消息有哪一些

比如说摄像头检测到老人摔倒了，摄像头程序会检测到，然后会推送消息到中控台；或者说摄像头的上下左右转动消息推送到摄像头。



### 多进程怎么使用

本项目的服务器部分是分为连接进程，业务进程，日志进程的，这么分配是为了防止一个模块挂掉就都挂掉，这样其他模块还能继续提供服务。



### 高性能日志介绍

因为日志进程接收并发量很高，所以我使用epoll来实现。其中需要用到日志的进程里都有一个日志发送类，这个类里面有一个静态函数，这个静态函数能将使用LogInfo类存储好的日志发送到日志进程里。发送函数是一个静态函数，里面有一个单独线程的client用于发送，日记进程就采用1个线程。



**select、poll、epoll有什么区别**
答：这三个都是IO多路复用，但是实现上有差距。select通过一个bitmap数组维护套接字，最大1024个，运行过程是使用一个数组维护套接字，调用时select函数时，系统会把这个数组拷贝到内核，内核轮询套接字检测有没有收到数据，有的就取消进程阻塞，并且将收到数据的套接字标记，然后重新拷贝到用户进程中，用户再遍历查看哪个收到数据。这样的操作有缺陷，首先套接字数量有限制，其次每次调用select函数都要遍历，需要O(n)的时间复杂度的轮询。poll的话通过链表来维护套接字，解决了select的套接字限制，但是核心的轮询问题还没有解决。epoll在内核中使用红黑树维护套接字，一个就绪队列用于存储就绪事件。红黑树方便查找套接字，然后讲收到数据的套接字通过引用的方式加入到就绪队列里。这样就使得进程不需要轮询查询收到数据的套接字，也避免了用户到内核的拷贝，改善了效率。



### 遇到什么问题，怎么解决

遇到一个问题是在**测试RTSP协议交互时，发现走到SETUP就没了**，而且DESCRIBE也没有出现，然后排查错误，打断点，发现**服务端负责客户端会话的session类出现问题**。原本连接之后，会使用**session类局部变量**来存放客户端，但是函数退出之后**局部变量就被析构掉**。因此我采用的解决方案是，**服务器接收到客户端请求后处理线程不立刻返回**，直到双方把数据发送完毕再返回，让**局部变量析构后于双方数据交互**。



RTSP第一个版本，后面改成UDP穿透实现P2P

直播遇到波动，缓冲区拉大，弱网环境下慢慢下载，再播放



## ffplay

**主流程**

1. `parse_options()`，解析命令行参数
2. `SDL_CreateWindow()`，创建 SDL 窗口
3. `stream_open()`，这个函数是**重中之重**，这个函数会创建4个线程，分别为`read_thread`、`audio_thread`、`video_thread`、`subtitle_thread`
4. `event_loop()`，这个函数是一个死循环，主要的任务就是不断先**播放视频帧**，再**处理键盘按键事件** 



### 命令行参数解析

`parse_loglevel`函数里通过指定命令名称，去命令行参数里匹配查看是否有指定参数



### stream_open函数

1. 创建VideoState结构体，
2. 初始化视频、音频和字幕队列的帧队列
3. 初始化视频、音频和字幕数据包的队列
4. 初始化视频、音频和外部时钟的时钟结构
5. 创建一个独立的读取线程`read_thread()` 是为了异步地从视频文件中读取数据，这样可以确保主线程或其他线程可以专注于解码和播放等任务，而不会被I/O操作阻塞



### read_thread函数

1. 定义数组`int st_index[AVMEDIA_TYPE_NB]`以存储每个媒体类型的流索引，涵盖了各种数据流，音频，视频，字幕，附件流等等。因为一个MP4里面可能会有多个视频流
2. `interrupt_callback` 指定了中断回调函数，在播放网络流的时候，网络卡顿，av_read_frame() 可能要 8 秒才能返回，这8秒内会定时执行中断回调函数
3. `avformat_open_input`打开输入媒体文件
4. 注入全局边数据
5. 查找流信息
6. 查找对应的流索引，设置`st_index`数组
7. 3次调用`stream_component_open`函数创建`audio_thread`、`video_thread`、`subtitle_thread`线程
8. `for (;;) {...}` 死循环不断 从 容器实例 读取 `AVPacket` ，然后丢进去对应的 `PacketQueue` 队列
   1. 解决是否暂停
   2. 如果当前是跳转操作，刷新3个队列的数据，且先填入刷新的数据包
   3. 判断3个队列缓存中的 `AVPacket` 是否都够用，或者3个队列的元素数量加起来是否超过`MAX_QUEUE_SIZE`，够用就会休眠 10ms
   4. `av_read_frame`从源文件中读取内容到pkt结构中
   5. 判断一下`AVPacket` 是否在期待的播放时间范围内
   6. 是就将该包加入到对应的队列里，否就丢弃该包

**pkt_in_play_range注意点：**

- `pkt_in_play_range` 来确定是否在播放时间范围内
- 对于`ffplay -i juren-5s.mp4`，因为 `juren-5s.mp4` 是一个 5 秒的视频，而且命令行没有指定 `-t`，所以这时候 播放时间范围 就是 0 ~ 5 秒。只要读出来的 `AVPacket` 的 `pts` 在 0 ~ 5秒范围内，`pkt_in_play_range` 变量就为真。因此所有读出来的 AVPacket 都是符合播放时间范围的。
- 但是如果加了 `-t` 参数，`ffplay -t 2 -i juren-5s.mp4`，该命令是只播放 2秒视频，也就是 播放时间范围 变成了 0 ~ 2 秒，如果读出来的 `AVPacket` 的 `pts` 大于 2 秒，就会被丢弃。
- 这里就有一个有趣的事情，当视频播放到第二秒的时候，虽然画面停止了，但是 `read_thread()` 还是会一直读数据，但由于不符合播放时间范围，会一直丢弃。直到读到文件结尾，返回 `AVERROR_EOF` 才会停下来休眠一小段时间。



### stream_component_open函数

1. `avcodec_alloc_context3()` 跟 `avcodec_parameters_to_context()` 申请一个解码器实例的内存，然后把容器流里面的信息拷贝过去。**容器里面通常都是有编码器信息的**。
2. 使用指定的编码器
3. `filter_codec_opts()` 把命令行参数的**相关参数**提取出来
4. `avcodec_open2()` 接受 `filter_codec_opts()` 返回 的 `AVDictionary` 参数
5. 流属性设置为不丢弃
6. 根据不同流的解码器设置，都是启动各自的解码函数线程

音频：

1. `configure_audio_filters()` 创建**音频流的滤镜**，搞好了 `is->in_audio_filter` 跟 `is->out_audio_filter` 两个滤镜。解码器输出 `AVFrame` 之后需要往 `in_audio_filter` 里面丢，然后播放的时候，需要从 `out_audio_filter` 读取 `AVFrame`。
2. `audio_open()` 函数的内部调 `SDL_OpenAudioDevice()` 打开音频设备，把**最终的**音频信息放在 `is->audio_tgt` 变量里面了。
3. `is->audio_src = is->audio_tgt;`设置音频源头的目标格式
4. 记录播放的音频流信息
5. 初始化并启动`audio_thread`



### video_thread、audio_thread、subtitle_thread

- Video Packet Queue消耗的函数调用关系：video_thread() -> get_video_frame() -> packet_queue_get(&is->videoq) 

- Video Frame Queue生产的函数调用关系：video_thread() -> queue_picture()      -> frame_queue_push(&is->pictq)
- 包在`decoder_decode_frame`函数里会被获取，然后进行解码，之后丢到入口滤镜里进行处理
- 处理完后丢到环形的帧队列里



### video_refresh函数

1. 如果时钟是外部时钟，检查外部时钟速度
2. 如果显示没有被禁用，并且显示模式不是视频，但存在音频流，播放音频的波形图
3. 如果视频队列中没有待显示的帧，则不执行任何操作，继续显示上一帧
4. 如果当前视频帧的序列号与队列中序列号的不匹配，则跳过此帧,直接从队列里面丢掉
5. 如果当前视频帧与上一个视频帧的序列号不匹配，则更新帧定时器
6. 如果处于暂停状态，则直接显示当前正在显示的视频帧
7. 计算上一个视频帧到当前视频帧的持续时间，并据此计算延迟
8. 根据计算出的延迟和当前时间判断是否需要等待或直接显示帧
9. 更新帧定时器
10. 如果队列中还有其他视频帧，且当前视频帧播放时间超过了预期时间，则跳过该帧
11. 处理字幕流
12. 如果force_refresh在上述操作被置1，把 上一帧视频帧 AVFrame 的数据渲染到 SDL_Texture（纹理）上面
13. 打印音视频的同步信息到控制台上



### 视频音频同步`compute_target_delay`函数

1. 判断当前是按照视频，还是别的，是别的才进入
2. 计算视频时钟与主时钟的差异
3. 计算同步阈值，该值是基于当前延迟`delay`和预定义的最大最小阈值 (`AV_SYNC_THRESHOLD_MIN(0.04)` 和 `AV_SYNC_THRESHOLD_MAX(0.1)` 动态确定的，范围是
4. 根据差异决定延迟：
   - 如果视频滞后于主同步源1个视频帧，视频追赶，立即播放
   - 如果视频快于音频的时间，比一帧视频的时间长，长多少时间就延长多少上一帧的显示时间
   - 如果视频快于音频1个视频帧以上，同时，视频流又是1秒5帧那种低帧率视频，直接delay变两倍



**跳转播放：**

1. 两种 seek 方式，一种是按时间 seek （默认），一种是 按字节 seek。
2. 按字节 seek，获取当前有有效的视频流，或者音频流播放到哪个字节位置了，再加上 incr （相对，跳转秒数）。
3. 按时间 seek，获取当前主时钟播放到第几秒了，然后再加上 incr （相对）。
4. `stream_seek()` 记录要跳转的位置，注意 `stream_seek()` 只是记录一下位置，并不会进行跳转操作。
5. read_thread函数里，在 调 `av_read_frame()` 之前，会检测一下 `is->seek_req` 是否为 1，为 1 代表有 seek 请求发生。
6. 调用`avformat_seek_file()` 函数把文件的读取位置，设置到离跳转的读取位置最近的关键帧的地方。
7. 用 `packet_queue_flush()` 清空之前队列里面的缓存，还有刷新序列号。
